# 后端 API 地址
#
# 用法：复制本文件为 .env（.env 已被 .gitignore 忽略，不会提交），
# 然后按需修改下面的值。
#
# Nuxt 的 runtimeConfig 会自动读取以 NUXT_ 开头的环境变量，
# 所以这里改了之后【不需要动任何代码】：
#   runtimeConfig.public.apiBase   ←  NUXT_PUBLIC_API_BASE
#   runtimeConfig.apiBaseServer    ←  NUXT_API_BASE_SERVER
#   runtimeConfig.public.mediaBase ←  NUXT_PUBLIC_MEDIA_BASE
#
# ---------------------------------------------------------------------
# ① 浏览器用的地址（会打进前端产物，所以【不能放密钥】）
#    本地开发：后端 Spring Boot 跑在 8082
#    （注意不是 8081 —— 8081 留给另一个项目「星途点评」）
# ---------------------------------------------------------------------
NUXT_PUBLIC_API_BASE=http://localhost:8082

# ---------------------------------------------------------------------
# ② 服务端专用的地址（不会打进产物）
#
#    文章详情页是服务端渲染的，请求发生在服务器上。
#    如果那时也去请求公开域名，就等于绕一圈公网 + DNS + Nginx
#    再回到同一台机器的后端；域名没配好时 SSR 会直接失败。
#    所以服务端直接走内网/本机地址。
#
#    本地开发：不填也行（默认就是 http://localhost:8082）
#    服务器上：填后端容器的内网地址，例如
#      NUXT_API_BASE_SERVER=http://backend:8082      （同一 compose 网络）
#      NUXT_API_BASE_SERVER=http://127.0.0.1:8082    （同机不同容器/进程）
# ---------------------------------------------------------------------
NUXT_API_BASE_SERVER=http://localhost:8082

# ---------------------------------------------------------------------
# ③ 大文件（背景视频 / 背景音乐）的地址前缀
#
#    这两个文件在仓库根目录的 static-media/ 里，【不参与构建】：
#    线上由 Nginx 的 location /media/ 直接读磁盘提供
#    （见 static-media/README.md，含上传步骤与 Nginx 配置片段），
#    dev 由 Nitro 的一个仅开发环境生效的路由提供 —— 两边路径都是 /media。
#
#    本地开发：不填也行（默认就是 /media）
#    服务器上：与 Nginx 的 location 前缀保持一致；哪天把文件挪到
#    OSS / CDN，改这一行即可（代码里没有第二处硬编码）：
#      NUXT_PUBLIC_MEDIA_BASE=https://cdn.example.com/media
# ---------------------------------------------------------------------
NUXT_PUBLIC_MEDIA_BASE=/media

# ---------------------------------------------------------------------
# ④ 站点的对外地址（SEO 用：canonical / og:url / sitemap.xml 的绝对地址）
#
#    canonical 与 og:url 规范上必须是【绝对地址】：写相对路径的话
#    搜索引擎会直接忽略这条 canonical，而页面上完全看不出异常 ——
#    属于"静默失效"，只在被搜到、被分享的时候才会发现。
#
#    本地开发：不填也行（默认 https://www.yigalaxy.xin）
#    服务器上：填真实域名，例如
#      NUXT_PUBLIC_SITE_URL=https://你的域名
#    测试环境想区分开时也一样覆盖（sitemap 里的地址会跟着变）：
#      NUXT_PUBLIC_SITE_URL=https://test.你的域名
#
#    值会被 app/utils/seo.ts 的 normalizeSiteUrl() 归一化：
#    去掉结尾多余的斜杠；空值 / 不以 http(s):// 开头的一律回落到默认域名
#    （拼不出合法绝对地址时，宁可回落到正式域名，也不要产出坏地址）
# ---------------------------------------------------------------------
NUXT_PUBLIC_SITE_URL=https://www.yigalaxy.xin
