# yiguixingtu-web — 个人博客前端

<!--
  CI 徽章：等这个仓库有了 GitHub 远程地址之后，把下面这行取消注释、
  并把 <用户名>/<仓库名> 换成实际的，即可显示 CI 状态。
  现在先不写死，是因为仓库还没有 remote，写上去只会显示一个坏掉的图片。

  [![CI](https://github.com/<用户名>/<仓库名>/actions/workflows/ci.yml/badge.svg)](https://github.com/<用户名>/<仓库名>/actions/workflows/ci.yml)
-->

> 基于 Nuxt 4 + Vue 3 + Element Plus 的个人博客前端
> 后端为独立仓库 `yiguixingtu`（Spring Boot 4，默认跑在 `localhost:8082`）
>
> **419 个测试用例 + ESLint + 生产构建，全部在 CI 里自动跑**（见下文「持续集成」）

## 项目简介

这是个人博客 **忆轨星途** 的前端站点，包含**前台展示**和**后台管理**两部分：

- **前台**：首页文章信息流（**服务端渲染** + 分页 + 关键词防抖搜索 + 分类筛选 + **标签筛选**，三个筛选条件都同步到地址栏）、
  文章详情（Markdown 渲染 + **文章标签** + **评论区**，同样是服务端渲染）、
  **归档页**（`/archive`：把全部已发布文章按年月重新排一遍，导航里可直接进入），
  个人卡片的文章数 / 浏览量 / 分类数来自站点统计接口
- **后台**：登录后进入 `/admin`，可管理用户（分页 / 启用禁用 / 改角色 / 重置密码 / 删除）、
  文章（Markdown 编辑器 / 草稿与发布 / 下架 / 删除 / **多选标签**）、
  **标签**（新建 / 改名 / 排序 / 删除，删除会同时解除文章关联）与
  **评论**（按状态筛选 / 通过 / 拒绝 / 删除，菜单上有待审核数量角标），
  另有**只读**的分类列表；概览页显示全站汇总数字

站点采用深色星空视觉，带背景视频与音乐播放器（这两个大文件**不进构建产物**，
由服务器上的 Nginx 从 `/var/www/media/` 提供，见下文「背景视频与音乐为什么不放在 `public/` 里」）。

**全站有完整的 SEO 元信息**：每页的 `title` / `description` / `og:*` / `canonical`、
`<html lang="zh-CN">`，后台页带 `noindex`；另有 `/sitemap.xml` 与 `/robots.txt`
（见下文「首页为什么要服务端渲染」与「全站 SEO 元信息」）。

## 技术栈

| 类别 | 技术 |
|------|------|
| 框架 | Nuxt 4（`^4.5.2`） |
| 视图 | Vue 3（`^3.5.42`） |
| 路由 | vue-router 5 |
| UI 组件库 | Element Plus 2.14 |
| Markdown 编辑器 | md-editor-v3 |
| 语言 | TypeScript |
| 包管理 | npm |
| 测试 | Vitest 5 + `@nuxt/test-utils` 4（nuxt 环境）+ `@vue/test-utils` |
| 代码检查 | ESLint 10 + `@nuxt/eslint` 1.17（扁平配置） |
| 持续集成 | GitHub Actions（`.github/workflows/ci.yml`） |

## 目录结构

```
yiguixingtu-web
├── app
│   ├── app.vue                      # 应用外壳：全局布局、登录/注册弹窗、背景视频与音乐
│   ├── error.vue                    # 全局错误页（404 / 403 / 401 / 500 分类文案）
│   ├── pages
│   │   ├── index.vue                # 首页：服务端渲染的文章信息流 + 搜索（防抖）+ 分类/标签筛选 + 加载更多
│   │   ├── archive.vue              # 归档：按年月浏览全部已发布文章（服务端渲染，顺序完全照搬后端）
│   │   ├── admin.vue                # 后台：用户 / 文章（+标签多选）/ 标签管理 / 只读分类 / 评论审核 + Markdown 编辑器
│   │   └── article/[id].vue         # 文章详情（SSR + Markdown 渲染 + 标签 + 评论区 + 软 404）
│   ├── composables
│   │   ├── useApi.ts                # 统一请求封装：带 token、判 body.code、401/403/429 处理、429 不自动重试
│   │   ├── useArticleFilter.ts      # 首页筛选条件：关键词防抖 + 分类 + 标签，三者与地址栏双向同步
│   │   ├── useSiteStats.ts          # 站点统计（文章数 / 浏览量 / 分类数）与失败降级
│   │   ├── useSeoMetaFor.ts         # 页面级 SEO 元信息的唯一入口（title / og / canonical / robots）
│   │   ├── useAuth.ts               # 登录 / 注册 / 当前用户（含被限流后的冷却）
│   │   ├── useRememberedLogin.ts    # 「记住用户名」cookie：只存用户名 + 清理老 cookie
│   │   ├── useAuthUi.ts             # 登录弹窗开关状态
│   │   └── useReveal.ts             # 滚动入场动画
│   ├── utils
│   │   ├── media.ts                 # 大文件（视频 / 音乐）的地址约定 mediaUrl()
│   │   ├── seo.ts                   # SEO 纯函数：标题 / 描述 / canonical / og 的拼装规则
│   │   ├── comment.ts               # 评论的纯规则：长度上限、校验、表单 → 请求体、列表归一化
│   │   └── apiError.ts              # 两种 429（限流 / 重复提交）的判断与文案、追踪号 X-Trace-Id、重试状态码白名单
│   ├── middleware
│   │   └── admin.ts                 # 后台路由守卫（未登录 → 弹登录框 + 回首页）
│   └── plugins
│       └── element-plus.ts          # Element Plus 注册
├── server
│   ├── routes/media/[...file].get.ts # 仅开发环境生效：把 /media/** 指向 static-media/
│   ├── routes/sitemap.xml.get.ts     # GET /sitemap.xml：运行时生成（分页取全部已发布文章）
│   ├── routes/robots.txt.get.ts      # GET /robots.txt：允许抓取 + 指向 sitemap + 挡掉后台
│   ├── utils/sitemap.ts              # XML / robots 的生成与"翻页取全部文章"（纯函数）
│   └── utils/mediaFile.ts            # 文件名 → 磁盘路径（穿越防护）+ Range 解析（纯函数）
├── public                           # 参与构建的小静态资源（封面图、favicon）
├── static-media                     # 【不参与构建】背景视频 / 音乐，部署时上传到 /var/www/media/
├── nuxt.config.ts                   # Nuxt 配置（后端 API 地址、媒体前缀、站点地址）
└── .env.example                     # 环境变量示例
```

## 快速开始

### 1. 环境要求

- Node.js 20+
- npm
- **后端服务已启动**（见后端仓库 `yiguixingtu` 的 README；默认 `http://localhost:8082`）
  - 后端依赖 MySQL 与 Redis，需要先 `docker compose up -d`

### 2. 安装依赖

```bash
npm install
```

### 3. 配置后端地址

复制环境变量示例文件并修改：

```bash
cp .env.example .env
```

默认内容：

```properties
NUXT_PUBLIC_API_BASE=http://localhost:8082
NUXT_API_BASE_SERVER=http://localhost:8082
NUXT_PUBLIC_MEDIA_BASE=/media
```

> Nuxt 的 `runtimeConfig` 按名字自动读取环境变量
> （`NUXT_PUBLIC_API_BASE` → `runtimeConfig.public.apiBase`，
> `NUXT_API_BASE_SERVER` → `runtimeConfig.apiBaseServer`，
> `NUXT_PUBLIC_MEDIA_BASE` → `runtimeConfig.public.mediaBase`），
> 所以改地址**不需要动代码**。

#### 为什么是两个地址，不是一个

**服务端和浏览器要用的后端地址经常不是同一个。**

| | 用哪个 | 为什么 |
|---|---|---|
| **浏览器** | `NUXT_PUBLIC_API_BASE`（会打进产物，**不能放密钥**） | 必须是对外可访问的地址，否则用户浏览器请求不到 |
| **服务端渲染** | `NUXT_API_BASE_SERVER`（不会打进产物） | SSR 的请求发生在服务器上，去请求自己的公网域名等于绕一圈 DNS + Nginx 再回到同一台机器，白白多几十毫秒；域名没配好时 SSR 还会直接失败 |

本地开发时两者都是 `http://localhost:8082`，看起来没区别；
但部署时就分开了，例如：

```properties
# 服务器上：浏览器走域名，SSR 直连内网容器
NUXT_PUBLIC_API_BASE=https://你的域名/api
NUXT_API_BASE_SERVER=http://backend:8082
```

> 选地址的逻辑在 `app/composables/useApi.ts` 的 `resolveApiBase()`。
> 它被单独抽成一个纯函数，是因为 `import.meta.server` 是**编译期常量**，
> 测试里没法两种情形都跑到；抽出来之后传一个布尔值就能把两条分支都测到
> （见 `test/useApi.nuxt.spec.ts` 的 5 个用例）。

### 4. 启动开发服务器

```bash
npm run dev
```

访问 `http://localhost:3000`。

> ⚠️ **前端必须跑在 3000 端口**：后端 CORS 白名单目前写死
> `http://localhost:3000`（后端 `SecurityConfig.java`），换端口就会跨域失败。

### 5. 构建与预览

```bash
npm run build      # 生产构建
npm run preview    # 本地预览生产构建
npm run generate   # 生成静态站点
```

### 6. 运行测试

```bash
npm run test         # 跑一遍（vitest run）
npm run test:watch   # 监听模式，改代码自动重跑
```

**不需要启动后端** —— 测试会把请求层 mock 掉，所以断网、后端没起也能跑。

### 7. 代码检查

```bash
npm run lint         # 检查（eslint .）
npm run lint:fix     # 自动修掉能修的部分
```

用 **`@nuxt/eslint`** 的扁平配置（Nuxt 官方推荐的那套），
在 `eslint.config.mjs` 里做了少量覆盖，每一条覆盖都写了为什么。

> **当前 lint 是干净的（0 error / 0 warning）**，所以 CI 上这一步是有意义的门槛；
> 如果放着几十条 warning 不管，大家很快就会习惯性忽略它。

### 8. 持续集成

`.github/workflows/ci.yml`，在 **push 到 master** 和 **PR** 时触发，三步依次执行：

| 步骤 | 命令 | 为什么单独一步 |
|------|------|---------------|
| 代码检查 | `npm run lint` | 串成一条命令的话，Actions 页面只会显示一句 exit 1，看不出是哪一步挂的 |
| 运行测试 | `npm run test` | 419 个用例；**不需要后端与数据库**，CI 里不用起任何服务 |
| 生产构建 | `npm run build` | 保证"测试过了但build 不过"这种情况不会漏到线上 |

用 `npm ci` 而不是 `npm install`：它严格按 `package-lock.json` 安装，
装不出锁文件之外的东西，所以"CI 绿了、别人 clone 下来却跑不起来"不会发生。

> ⚠️ **本仓库目前还没有 GitHub 远程地址**，所以工作流文件已就位但还没真正跑过。
> 建好远程仓库、推上去之后，README 顶部的 CI 徽章按注释里的说明替换 URL 即可。

## 部署

### 方式一：交给后端仓库的 compose 统一编排（推荐）

本仓库提供了 `Dockerfile`（多阶段构建的 Nuxt 4 SSR 镜像）。
上线时**不需要单独起它** —— 后端仓库的 `docker-compose.prod.yaml` 里已经包含
一个 `frontend` 服务，会构建本镜像，并和后端、数据库一起编排：

```bash
# 默认假设两个仓库是同级目录，例如 /srv/yiguixingtu 与 /srv/yiguixingtu-web
cd /srv/yiguixingtu
# 准备好 .env（变量清单见后端仓库 README 的「部署」章节）
docker compose -f docker-compose.prod.yaml up -d --build
```

一次就把 4 个容器都起起来（mysql / redis / backend / frontend），
并且在同一个内网里 —— 前端用服务名 `backend` 直接访问后端。

> 前端仓库不在同级目录时，在 `.env` 里指定 `FRONTEND_DIR=/你的/实际/路径`。

### 方式二：单独构建与运行

```bash
docker build -t yiguixingtu-web .

docker run -d --name yiguixingtu-web \
  -p 3000:3000 \
  -e NUXT_PUBLIC_API_BASE=https://你的域名/api \
  -e NUXT_API_BASE_SERVER=http://后端地址:8082 \
  yiguixingtu-web
```

### 两个环境变量的分工（**部署时最要紧的一点**）

镜像只构建一次，两个地址在**运行时**用环境变量注入：

| 变量 | 谁在用 | 填什么 |
|---|---|---|
| `NUXT_PUBLIC_API_BASE` | **浏览器** | 对外可访问的后端地址，例如 `https://你的域名/api`。它会打进前端产物，**不能放密钥** |
| `NUXT_API_BASE_SERVER` | **服务端渲染** | 后端的内网地址，例如 `http://backend:8082`。SSR 直连内网，不用绕公网域名 |

**为什么必须分开**：文章详情页是 SSR 的，请求发生在服务器上。
如果那时也去请求公开域名，等于绕一圈 DNS + Nginx 再回到同一台机器，
白白多几十毫秒；域名没配好时 SSR 还会直接失败。
（选地址的逻辑在 `app/composables/useApi.ts` 的 `resolveApiBase()`。）

**镜像里没有 nginx**：Nuxt 的构建产物 `.output/server/index.mjs` 本身就是
一个 Node 服务，同时负责渲染页面和提供静态资源，对外统一由宿主机的 Nginx 反代。

**镜像里也没有背景视频与音乐**：这两个大文件在仓库的 `static-media/` 里
（同时被 `.dockerignore` 排除），部署时单独上传到宿主机 `/var/www/media/`，
由 Nginx 的 `location /media/` 提供 —— 换一次背景视频不需要重新构建镜像。
细节见上文「背景视频与音乐为什么不放在 `public/` 里」。

### 上线前的检查

| # | 检查项 |
|---|---|
| 1 | `NUXT_PUBLIC_API_BASE` 是真实域名，且与后端的 `CORS_ALLOWED_ORIGINS` 对得上 |
| 2 | 浏览器打开站点，F12 里没有跨域报错 |
| 3 | 文章详情页与**首页**"查看源代码"都能看到正文 / 文章列表（说明 SSR 生效，对 SEO 很重要） |
| 4 | 后端地址用的是服务名/内网地址，不是 `localhost`（容器里的 `localhost` 指容器自己） |
| 5 | **`static-media/` 里的文件已经传到服务器 `/var/www/media/`**，浏览器直接打开 `https://你的域名/media/bg-music.mp3` 能播放（能播就说明 Nginx 的 `location /media/` 生效、且没被 `location /` 抢走） |
| 6 | `NUXT_PUBLIC_SITE_URL` 填的是**真实域名**：`curl -s https://你的域名/ \| grep canonical` 出来的应该是那个域名，而不是默认的 `www.yigalaxy.xin`（填错的表现是"canonical 指向别人的域名"，等于把收录送出去） |
| 7 | `curl -s https://你的域名/robots.txt` 能拿到内容（**不是 404**）：它是 Nitro 的运行时路由，必须由反代转发给 Node；同时确认里面的 `Sitemap:` 是真实域名 |
| 8 | `curl -s https://你的域名/sitemap.xml` 返回 `200 + application/xml`，且**文章条数对得上**（不是只有首页那一条 —— 只有首页通常意味着后端地址配错、取数降级了） |
| 9 | `curl -s https://你的域名/archive` 能看到按年月分好组的文章（**在 HTML 里**，不是靠 JS 补的），导航里也能点到「归档」 |


**测试环境**：Vitest 5 + `@nuxt/test-utils` 4，跑在 **nuxt 环境**而不是裸的 jsdom。

> **为什么必须用 nuxt 环境？** 被测代码用的是 Nuxt 自动导入的 API
> （`useRuntimeConfig` / `useCookie` / `useState` / `$fetch`），
> 这些在裸 node / jsdom 里根本不存在。用 nuxt 环境测的是"代码在 Nuxt 里的真实行为"，
> 而不是把所有依赖都 mock 掉自己骗自己。

**当前 23 个测试文件、419 个用例：**

| 测试文件 | 用例数 | 覆盖 |
|---------|:---:|------|
| `test/useApi.nuxt.spec.ts` | 35 | 请求封装的**六条失败分支**（业务 code≠200 / 幂等重复提交 / 401 / 403 / **429 限流** / 网络异常）、**两种 429 的提示必须不同**、429 不清 token、token 是否带上、baseURL 与参数透传、`resolveApiBase` 服务端与浏览器两种地址；**重试策略**：用**假的 fetch 实现驱动真 ofetch**，429 只打一次、去掉我们的配置后会打两次（对照组）、503 仍然重试一次、POST 不重试、传给 `$fetch` 的必须是**数组**（传 Set 会被静默忽略）；以及**追踪号**：业务失败（靠 ofetch 的 `onResponse` 钩子拿响应头）与 HTTP 429/500 都要带上 `X-Trace-Id`、没有这个头时**不出现"追踪号"三个字也不出现空括号**、成功时一条提示都不弹 |
| `test/useAuth.nuxt.spec.ts` | 17 | 登出链路：**会调用后端** `POST /auth/logout`、带上当前 token、清空 `token` 与 `user`、接口失败也照样清本地、不再跳到不存在的 `/login`；**登录被限流**：认得出 HTTP 429、提示里要有"频繁"、没有 body 时用兜底文案、冷却是 10 秒且逐秒递减、冷却期间**不再发请求**、冷却结束后能正常登录、连点两下（一次限流一次成功）以成功为准；**追踪号**：限流时带上 `X-Trace-Id`、断网拿不到时不出现"追踪号"；密码错误与网络异常都**不能**被误判成限流 |
| `test/useAuthUi.nuxt.spec.ts` | 6 | 登录/注册弹窗开关的**互斥**、`closeAll`、跨组件共享同一份状态 |
| `test/useUpload.nuxt.spec.ts` | 16 | 图片上传：扩展名白名单（含大写、无扩展名、脚本文件）、大小边界（正好等于上限 / 超一字节）、空文件；以及请求拼装——**字段名必须是 `file`**、**不能手动设 Content-Type**（设了会丢 boundary）、401 不抛异常 |
| `test/useArticleFilter.nuxt.spec.ts` | 39 | 首页筛选条件：地址栏 → 状态的解析（非法 `categoryId` / `tagId`、重复键、trim）、状态 → 地址栏的序列化（空值不写）、状态 → 后端参数的拼装；以及**防抖**（299ms 不生效 / 300ms 生效、连续输入只生效最后一次）与**双向同步**（后退同步回状态且不反向写一次、连点同一分类不重复写）。**标签（tagId）是第三个维度**，所以对它重复了一整套对称的边界断言：点标签写地址栏、与分类叠加、再点一次即取消、换标签不叠加、后退时标签也要跟着清掉 —— 重复本身就是守住"两套逻辑不许分家" |
| `test/index.nuxt.spec.ts` | 28 | 挂载整个首页：分类按钮渲染与高亮、点分类后**请求参数 + 地址栏 + 标题**三处同步、筛选变化回到第 1 页、**带 `?keyword=&categoryId=` 的地址打开等于刷新**（输入框回填、首屏请求就带条件）、输入防抖（299ms 不发请求）、分类接口失败/返回非数组时**不崩只是不显示筛选条**；以及个人卡片的三个数字来自 `GET /article/stats`（列表里两篇浏览量合计只有 8，卡片显示 28 才说明不是当前页求和）、统计接口失败显示「—」而不是 0；**标签**：筛选条渲染（与分类是两个类名，互不干扰）、点筛选条与点卡片上的标签都按 `tagId` 请求、点卡片标签**不会跳进详情页**、换标签会**重新发请求**（key 里不带 tagId 时一个请求都不会发，只断言参数是抓不到这个 bug 的）、带 `tagId` 的地址打开能恢复高亮、三个条件一起被「清除筛选」清掉。（首页改成 SSR 之后，这个文件里每条用例结束都要**卸载组件**，理由见下文「首页为什么要服务端渲染」） |
| `test/archive.nuxt.spec.ts` | 14 | 归档页（`/archive`）：按月份分组渲染标题 / 篇数 / 文章、每条都是可爬的站内链接（`href` 指向 `/article/{id}`）、页面只印"日"（年月已在段标题里）；**顺序完全照搬后端** —— 故意喂一个"乱序"的 months 与"旧的在前面"的月内列表，断言输出顺序与输入**逐字一致**（前端要是顺手 sort 了一遍，页面上完全看不出异常，只有顺序不对，所以这条必须用乱序输入来钉）；`count` 用后端给的值、缺失时回落到数组长度、`total` 是字符串或负数时收成能显示的数字（不出现 `NaN` / `-1 篇`）；空数据与**接口失败是两句不同的话**（"还没有发布任何文章" vs "归档暂时读不到" + 回首页的出口），失败时页面骨架还在（不白屏、不是错误页）、也不谎报总数；`data`/`months` 结构不对时当成空归档而不是把渲染打崩；请求只打一次 `/article/archive` 且**不带任何参数**（带参数说明前端在替后端筛选/排序） |
| `test/articleTag.nuxt.spec.ts` | 5 | 文章详情页的标签：标题上方渲染出 `#标签`、**第一帧就有**（SSR 的诉求）、没有标签时整块不渲染、`tags` 是 null 时当成没有标签而不是把正文页带崩、`href` 指向 `/?tagId=`（内链可爬、可中键新开）、**点标签跳回首页且地址栏带上 tagId**（详情页自己没有筛选能力，"按标签看"只能落到首页那套筛选状态上） |
| `test/comment.nuxt.spec.ts` | 30 | 评论的**纯规则**：长度上限与后端 `CommentForm` 的注解逐字对齐（50 / 100 / 1000）且对象被冻结；校验的每一条边界（昵称正好 50 通过 / 51 拒、内容正好 1000 通过 / 1001 拒、首尾空格 trim 后再算长度、只有空格算没填、多处不合法时只返回第一个）；**邮箱校验故意比后端松**（`a@b`、`reader+tag@…`、`中文@…`、`o'brien@…` 都不该被前端拦下 —— 前端更严会把合法邮箱挡在门外，而用户没法绕过前端）；`normalizeCommentForm` **邮箱没填时整条字段都不出现**（发空串会让库里存一个 `""`）、三处都 trim、`articleId` 转数字；`normalizeCommentPage` 的失败降级（ok:false / data 为 null / records 不是数组 / total 是字符串负数或缺失） |
| `test/articleComments.nuxt.spec.ts` | 22 | 挂载详情页测**评论区**：空状态与列表渲染（昵称 / 时间精确到分钟 / **保持后端的时间正序** / 评论数）、接口失败或返回非数组时只让评论区空着而**正文照常**；**安全**：后端转义过的内容原样显示实体码（**不反转义**），即使内容里是原生 HTML 标签也只显示成文字（**证明用的是 `{{ }}` 而不是 `v-html`**）；提交成功时**必须提示"等待审核"**、把刚提交的那条显示在"仅你可见"那一块并标上「待审核」、不混进公开列表、内容清空而昵称保留；`createTime` 为 null（联调实测的响应形状）时显示「刚刚」；失败分支逐条走对 —— 本地校验拦住不发请求、400/404 显示后端原话、**真 HTTP 429 显示"请求过于频繁"且提示里不出现"网络"**、429 且响应没有 body 时有兜底文案、断网提示网络异常且页面不崩、**提交失败不清空用户刚写的内容**；提交中按钮禁用并显示"提交中…"；**加载更多**：下一页页码算对（第一页不满一整页时不能把第 1 页又拉一遍 —— 这个 bug 就是被这条用例抓出来的）、取完时不显示按钮、下一页失败时保留已显示的评论 |
| `test/useSiteStats.nuxt.spec.ts` | 17 | 站点统计：请求路径与参数、三个字段的**归一化**（`data` 为 null / 缺字段 / 字符串 / 负数一律收成能显示的数量，不能出现 NaN）、**失败降级**（业务 code≠200 / HTTP 500 / 请求层直接抛异常都不抛给页面，数字保持 0 并立起 failed）、成功后再次失败**保留旧数字**、失败后重试能恢复 |
| `test/admin.nuxt.spec.ts` | 8 | 挂载整个后台：进入 `/admin` 就**主动**请求统计接口（不用先点「概览」菜单）、概览四张卡片显示真实数字、文章数标明"已发布"口径且不再有写死的「标签 0」、切到概览会再拉一次、在用户管理里筛选**不影响**概览的用户数、统计接口失败/抛异常时显示「—」且页面其余部分照常 |
| `test/adminTags.nuxt.spec.ts` | 16 | 后台**标签管理**：列表显示名字 / 排序 / 已发布文章数（**0 也显示 0**，不是「—」——它是后端算出来的确定答案）、接口失败或返回非数组时列表为空但页面不崩；新建走 `POST /admin/tag` 且成功后**重新拉列表**（断言新标签出现在表格里）、**名字前后空格会被 trim 再提交**、只有空格时前端就拦下来不浪费一次往返；**重名**时后端那句「标签名已存在」要显示在**弹窗里**且弹窗不关闭（toast 三秒后消失，而用户要做的动作就是改名再点一次保存）、后端没给原因时有兜底文案而不是一个空白错误条；编辑走 `PUT /admin/tag/{id}` 且回显当前值、`sort` 为 null 时回显成 0（否则用户一保存就悄悄改了它）；删除**先二次确认**且确认文案里写明"会解除文章关联、无法恢复、影响几篇文章"、点取消一个请求都不发、成功后列表刷新、**404（已被别人删掉）时提示"列表已刷新"并真的重拉**（自愈，而不是让用户对着报错再点一次）；分类页**只读**（有列表与说明，且**一个写按钮都没有**）、从标签管理切到分类不重复请求（两页共用同一份分类列表） |
| `test/adminComments.nuxt.spec.ts` | 20 | 后台**评论审核**：默认按「待审核」筛选（`status=0` 真的发给了后端）、列表显示昵称 / 内容 / 文章标题 / 时间 / 邮箱 / IP、内容按**纯文本**渲染（`v-html` 在这里同样是 XSS 入口）、接口失败或返回非数组时不崩、切到「已通过」会带 `status=1` 重新查询（**真实交互**：点开下拉框再点选项）、选「全部」**不发 status 参数**（也不能把界面哨兵值 -1 发给后端）；**审核**：通过/拒绝走 `PUT /admin/comment/{id}/status`，且状态必须是**查询参数**而不是请求体（后端是 `@RequestParam`，放 body 里会 400 —— 这条断言就是钉它的）、成功后刷新列表、已是当前状态的按钮禁用；**角标**：进后台就显示待审核数量、为 0 时整个角标不渲染、在「已通过」里拒绝一条时会**额外查一次**待审核数（列表的 total 不代表待办数）；**删除**：二次确认里说明"前台也看不到了、无法恢复"、点取消不发请求、成功后那条消失；**404 自愈**：审核与删除撞上 404 都提示"已经不在了 / 列表已刷新"并真的重拉；文章被删除时"文章"列显示占位说明而不是空白 |
| `test/adminArticleTags.nuxt.spec.ts` | 7 | 后台文章弹窗里的**标签多选**：选项来自 `GET /admin/tag/list`（后台那份不走缓存，刚建的标签立刻能选到）、新建时**不能带着上一篇的标签**、编辑时用**详情**里的 `tags` 回显（列表那份可能已经过期，用它回显会把别人刚打的标签覆盖掉）、详情接口挂了时退回用列表里的那一份（**绝不能显示成"一个都没选"**，否则用户一保存就把标签清空）、选了标签保存时 `tagIds` 是选中的 id 数组、**一个都不选时显式提交 `[]` 而不是不传这个字段**、标签接口失败时下拉框为空但弹窗与保存照常。用例走**真实 DOM** 点 el-select（点开下拉框 → 在属于标签那一个面板里点选项），因为页面上有 5 个下拉框且 Element Plus 会把面板 teleport 到 body，按内容认面板才不会点到别人的选项 |
| `test/homeContent.nuxt.spec.ts` | 11 | 首页**内容真实性**：正常有数据与"后端什么都没有"两种情况下面页都不出现任何假字符串（访客A / 1 人正在看 / 三个编出来的曲名 / CLOUD MUSIC / RSS / GitHub），假的留言浮窗与在线人数连节点与样式一起删掉、音乐卡片只指向真实存在的 `/media/bg-music.mp3` 且没有上一首/下一首、`#music` 锚点与播放/暂停/播完切回功能照常、真内容（文章列表、分类、卡片数字、时钟面板）没被误删 |
| `test/media.nuxt.spec.ts` | 35 | 媒体地址与 dev 媒体路由：`mediaUrl()` 要拼出 `/media/xxx`、**可以**被运行时配置覆盖（换 CDN）、空值回到默认前缀、非法文件名（斜杠 / 反斜杠 / `%2e%2e%2f` / 隐藏文件 / 空名字）一律拒绝；`resolveMediaFile()` 的**路径穿越防护**（上跳、子目录、换扩展名、空字节共 12 种变形都拒绝，且解析结果必须落在媒体目录内）；`parseRangeHeader()` 的边界（`bytes=100-`、`bytes=-500`、终点越界截断、起点越界回 416、多区间与畸形头当作没有 Range） |
| `test/loginRateLimit.nuxt.spec.ts` | 7 | **挂载整个应用外壳**测登录被限流时界面到底什么样：提示是"太频繁"而不是"网络异常"、登录按钮真的 `disabled` 且按钮上写着还有多少秒、冷却期间连点**不再发请求**、**按回车提交也被拦**（回车绕得过 disabled）、倒计时结束后按钮自己活过来、成功与密码错误都不进入冷却 |
| `test/useRememberedLogin.nuxt.spec.ts` | 16 | 「记住用户名」：构造出来的 cookie **只含 `username`**（白名单断言，写不出 password）、用户名为空时不写 cookie；读回填兼容对象 / JSON 字符串 / 纯字符串三种形态；**老 cookie（带明文密码）在挂载时被主动改写成只含用户名**（对象与字符串两种形态都测）、新格式不做无意义写入；**挂载整个外壳**验证：浏览器里那条带密码的 cookie 被清掉、输入框只回填用户名而**密码框是空的**、勾选后登录写入的 cookie 里不含密码、没勾则删掉 cookie |
| `test/linkUnderline.nuxt.spec.ts` | 4 | `el-link` 的 `underline` **不再传废弃的布尔值**：外壳里两个链接（登录弹窗的「去注册」、注册弹窗的「已有账号？去登录」）拿到的 prop 都是字符串 `'never'`、**渲染它们时一条 `ElementPlusError` 都不打印**（改之前会打 2~3 条）、类名与改之前一致（`never` 既不带 `is-underline` 也不带 `is-hover-underline`）；并配了一条**对照组**：直接给 `ElLink` 传 `underline: false` 时确实会打印那段警告 —— 否则一个从未被触发过的 `console.warn` 间谍会让"没有警告"永远为真 |
| `test/seo.nuxt.spec.ts` | 20 | 全站 SEO 元信息。**纯函数层**（15 条）：首页/文章页/后台页三种页面的 title、description、canonical、四条 og 的拼装；**没有封面时整条 `og:image` 都不出现**（不是空值）、有封面时把 `/uploads/x.png` 拼成绝对地址、封面本来就是绝对地址时原样保留；标题/描述为空时回落站点默认值（不能出现空 `title`）；站点地址的归一化（去掉尾斜杠、空值/`www.x.com` 这种缺协议的值一律回落到默认域名，否则会产出被搜索引擎忽略的相对 canonical）；后台页带 `noindex, nofollow`、普通页面不带；每页都带 `lang="zh-CN"`。**组件层**（5 条）：首页挂载后 head 里**真的有** title / description / canonical / og（读 `document.head`）、带筛选参数的地址 canonical 仍指向干净的 `/`、**首屏渲染就已经带着文章列表**（改成 SSR 的核心诉求）；详情页的 `og:type=article`、标题跟着文章走、封面拼成绝对地址、canonical 指向文章自己；后台页带 noindex |
| `test/sitemap.nuxt.spec.ts` | 38 | `sitemap.xml` 与 `robots.txt` 的纯函数。**XML 转义**：五个特殊字符都要转义、`&` 必须最先替换（否则双重转义）、标题里的 `&`/`<` 不会让 sitemap 非法、`loc` 里的 `&`/`'` 会被转义；**分页取全部文章**：total=0 只请求一次、**恰好 50 条只请求一次**、51 条翻两页（最后一页不满）、120 条翻三页；`total` 明显不对时靠"空页"停下、后端永远返回满页时在**页数上限**停下（防死循环）并标记 `truncated`；**降级**：接口失败/抛异常/返回结构不对都不抛异常，保留已取到的部分、返回的仍是**合法且含首页**的 XML；按 id 去重、没有 id 的记录丢掉（不拼 `/article/undefined`）；`lastmod` 只用日期、拿不到就整条不写；**归档页也在静态页里**（它是导航里点得到的独立入口，漏掉不会有任何报错，只是爬虫得先发现链接）；robots.txt 的 `Disallow: /admin` 与绝对地址 `Sitemap:`、站点地址归一化与 canonical 同源 |

**为什么先测这几个**：
- `useApi` 是全部请求的唯一出口，页面自己不做错误处理，全靠它返回的 `ok` / `code`。
  它一旦写错，表现是"页面没反应"或"提示文不对题"——比如把 401 当成网络异常，
  用户永远只看到"网络异常"，真正的原因（登录过期）不显示。
  同样地，**把 429 限流当成网络异常**（这次修的）会把用户往错的方向指：
  服务端一切正常，只是让他等一会儿。
  重试策略是同一个出口上的另一处"看不出来"：ofetch 默认会再打一次，
  或者配置写成了 `Set`（`Array.isArray` 不成立，静默回落默认值），
  两种写法都不报错，只是**后端先被多打了一倍**才被人发现 ——
  所以这里用假 fetch 驱动真 ofetch，直接数它被打了几次。
- `useAuthUi` 只有 20 行，但两个弹窗的互斥是它唯一且关键的不变量，
  坏了会出现两个弹窗叠在一起，肉眼回归很容易漏。
- `useUpload` 的两个坑在界面上都表现为一句"上传失败"，看不出原因：
  字段名写错后端收不到文件；手动设了 `Content-Type` 会丢掉 boundary 导致解析失败。
  用断言把"发出去的请求长什么样"钉住，改坏了立刻红。
- `useArticleFilter` 的三类错误（不防抖 → 按字发请求、只同步一个方向 → 地址栏和输入框
  各说各话、空值照写 → 地址栏里全是 `?keyword=`）在界面上都不显眼，
  但都会被用户直接感受到，所以用时间可控的假时钟把边界卡死。
- `index.nuxt.spec.ts` 是**第一个组件测试**：组合式函数测的是"逻辑对不对"，
  它测的是"页面把逻辑接上去了没有"——v-model 绑错变量、按钮忘了传参这类问题
  编译器管不了，只测 composable 也管不了。
- `useSiteStats` 的两个行为在界面上都看不出来（接口一直正常的话永远遇不到）：
  失败时到底是显示 0 还是占位、接口给了 null / 字符串 / 负数时会不会在页面上
  印出 NaN。数字不对是最难被发现的一类 bug，所以用断言钉死。
- `comment.nuxt.spec.ts` 守的是**规则本身**（长度上限必须与后端注解逐字对齐、
  邮箱校验宁可松也不能更严）；`articleComments.nuxt.spec.ts` 守的是**用户到底知不知道
  自己发成功了**：提交成功必须说"等待审核"、失败必须区分"参数错 / 限流 / 断网"
  （把限流说成网络异常会让人一直重试，越试越糟）、内容必须当纯文本渲染。
  这几条在界面上都不显眼：评论发出去了却没提示，用户的第一反应是"再发一遍"。
- `admin.nuxt.spec.ts` 守的是**页面接线**：概览的数字必须来自独立请求，
  而不是"你点过哪些菜单、筛过什么条件"。这种"看着有数、其实是别的数"的问题
  只能靠挂载整个页面来测。
- `adminTags.nuxt.spec.ts` 守的是**"看起来成功了"这一侧**：建完不刷新列表
  （用户以为没保存成功，再点一次就撞重名）、重名只弹一个三秒后消失的 toast
  （用户还在看弹窗，不知道该怎么办）、删除不说明"会解除文章关联"
  （用户以为只是从标签库里去掉，结果文章上的标签也没了）、
  名字带空格照原样提交（后端 trim 后入库，前端却按带空格的算长度）。
  这类"操作其实失败了、但界面看起来像成功"的问题，只有把请求体和界面残留一起断言才守得住。
- `adminComments.nuxt.spec.ts` 守的是**"待办不会被漏掉、也不会假装处理了"**：
  默认筛选必须是「待审核」（默认「全部」会让新评论淹没在已处理的评论里）、
  审核用的状态必须是**查询参数**而不是请求体（后端是 `@RequestParam`，放 body 里会 400）、
  在「已通过」里拒绝一条时角标要跟着变（列表的 total 不代表待办数）、
  404（已被别人删掉）要自愈而不是让用户对着报错再点一次。
  这些错法在界面上都表现为"点了没反应"或"数字对不上"，肉眼回归几乎发现不了。
- `homeContent.nuxt.spec.ts` 守的是**"删掉的东西不会再回来"**：
  假内容被删之后，没有任何类型检查或 lint 规则拦着它被重新加回去
  （"让首页看起来热闹一点"这个诱惑一直都在），所以用断言把
  "页面上不出现这些字符串"钉死；它同时断言真内容还在，
  免得哪天有人为了清理假数据把真功能一起删了。
- `media.nuxt.spec.ts` 守的是**媒体地址这一条约定**（`/media/<文件名>`）：
  前端负责拼地址、服务端（dev 路由）负责按地址找文件，两边必须一致。
  更要紧的是**路径穿越**：`../` 这类输入在正常使用中永远不会出现，
  也就永远不会被人发现 —— 所以用 12 种变形（斜杠、反斜杠、URL 编码、
  隐藏文件、换扩展名、空字节）把"必须拒绝"钉死，连同 Range 请求的边界
  （写错一个字节会让视频偶尔卡一下，肉眼几乎查不出来）。
- `loginRateLimit.nuxt.spec.ts` 守的是**"提示文要对，按钮要真的拦住人"**：
  composable 测的是"认出 429 了、冷却开了"，而用户真正碰到的是界面 ——
  按钮是不是真的灰了、上面写着还有多少秒、冷却期间连点和**回车**会不会又打一次后端。
  这类"接错线"的问题（composable 全绿、页面照样能点）只能挂载整个外壳才测得到，
  而登录弹窗就在外壳里（首页测不到它）。
- `useRememberedLogin.nuxt.spec.ts` 守的是**"密码不许落盘"这件事本身**：
  把密码写进 cookie 是那种"能跑、能记住密码、用起来还挺方便"的实现，
  没有任何工具会拦着它被重新写回去 —— 所以这里用白名单把
  "写出去的 cookie 只能有 username 这一个键"钉死，并额外测了
  "老 cookie 必须被主动清理"（改代码不改数据 = 没修）。
- `seo.nuxt.spec.ts` 守的是**"页面上看不出来的东西"**：title 拼错、canonical 写成
  相对地址、`og:image` 拼出一个空值，本地打开页面全都正常，只有被搜到、
  被分享的时候才发现不对 —— 所以规则那一层用纯函数钉死（含"没有封面时
  整条 `og:image` 都不出现"这种边界），接线那一层断言 head 里真的有那些标签。
- `sitemap.nuxt.spec.ts` 守的是**"没有任何人会去打开的那些文件"**：
  sitemap 少了一批文章、XML 里有个没转义的 `&` 导致整份非法、
  robots.txt 里 `Sitemap:` 写成了相对地址 —— 这三种情况在浏览器里
  要么看不出来、要么看起来完全正常，唯一的信号是"几周后搜不到"。
  翻页边界（50 / 51 / 120）尤其值得钉：**只有几十篇文章时，
  "不翻页"和"翻页"的结果一模一样**，等文章多起来才会发现第 51 篇之后再没被收录。
  降级那几条守的是"接口挂了也不能 500、不能空 body"——
  爬虫收到 500 反复重试、收到空 body 当非法，比"少收录几篇"严重得多。
- 追踪号那几条守的是**"提示里到底有没有那串号"**：头名字写错（比如写成
  `X-B3-TraceId`）、或者只盯着 catch 分支而漏掉 HTTP 200 的业务失败，
  两种写法都不会报错，只会让用户永远看不到追踪号 ——
  而这种"少了一个号"的缺失，只有在真出事、需要排查的时候才会被发现。
- `linkUnderline.nuxt.spec.ts` 守的是**"控制台警告不许再回来"**：
  废弃 API 的警告本身不影响功能，所以没有任何工具会拦着它被写回去 ——
  危害恰恰在于"不影响功能"：stderr 每次都刷一遍，真正的报错混在里面就被忽略了
  （本项目的「已知待办」里正是因为同类警告吃过一次亏）。
  写法和上一节的对照组同理：先证明"传布尔值确实会打警告"，
  再说"现在的写法一条都不打"，否则一条永远为真的断言什么也守不住。

> 组件测试用 `mountSuspended(组件, { route: '/?keyword=nuxt' })` 造"刷新页面"的场景；
> 断言地址栏时不能只 `await flushPromises()`——`router.replace()` 还要过一遍导航守卫
> （Nuxt 的 navigation-repaint 插件会在里面等一次 `requestAnimationFrame`），
> 所以测试里有一个 `settleRoute()` 专门多等一个宏任务。

## 页面与功能

| 页面 | 路由 | 说明 | 需要登录 |
|------|------|------|:---:|
| 首页 | `/` | 文章信息流（**服务端渲染**，列表在服务端 HTML 里）、关键词搜索（300ms 防抖）、分类筛选、**标签筛选**、三个筛选条件同步到地址栏、加载更多分页；文章卡片上带这篇文章自己的标签（点了就是按标签筛）；个人卡片的文章数 / 浏览量 / 分类数来自 `GET /article/stats` | 否 |
| 归档 | `/archive` | 按**年月**浏览全部已发布文章（**服务端渲染**）：每个月一段，段内是「日 + 标题」的紧凑列表，每一条都是可爬的站内链接；月份与文章顺序**完全照搬后端**（前端一次都不排）；接口挂了降级成"读不到"提示而不是白屏 | 否 |
| 文章详情 | `/article/:id` | Markdown 正文渲染（SSR）、**文章标签**（点了回首页按标签筛）、**评论区**（发表需审核后可见），已发布文章可访问 | 否 |
| 后台 | `/admin` | 用户管理 + 文章管理（文章可多选标签）+ **标签管理**（增删改）+ **评论审核** + **只读分类列表**；概览显示全站汇总数字（进入页面即主动加载）；head 带 `noindex` | **是（ADMIN）** |

后台的左侧菜单有七项：概览 / 文章管理 / 用户管理 / 标签管理 / 分类 / 评论管理 / 设置。
「评论管理」那一项在有待审核评论时会带一个数字角标（进后台就看得见"有几条要处理"）。
「分类」目前是**只读**的（后端其实已经有分类的增删改接口，是前端这一批没接），
「设置」仍然是占位 —— 详见下文「后台的标签管理与只读分类」与「后台的评论审核」。

首页的**搜索与筛选**是一套状态、一次请求：
关键词、分类、标签一起写进地址栏（`/?keyword=nuxt&categoryId=2&tagId=3`），也一起发给后端
（`GET /article/page?page=&size=&keyword=&categoryId=&tagId=`，后端按 AND 叠加）。
带着这样的地址刷新、或者把链接发给别人，打开时筛选状态会原样恢复。
细节见下文「搜索、分类与标签筛选为什么要这么写」。

后台文章编辑弹窗里的**封面是上传的**（不是填 URL）：
点"上传封面"选图 → 前端先做类型与大小预检 → 传到 `POST /upload` →
把返回的 URL 回填到表单 → 保存文章时一起提交。右侧有预览和"移除"。

> 前端预检只是**体验优化**（本地即时反馈、不浪费用户的上行带宽），
> 真正生效的是后端那一层——前端代码可以被绕过（直接调接口）。
> 两边的规则保持一致：`jpg / jpeg / png / gif / webp`，单张 ≤ 5MB。

全局交互：

- 首页右上角可打开**登录 / 注册弹窗**
- 未登录访问 `/admin` 会被路由守卫拦下并弹登录框
- 顶部导航栏在登录后显示昵称，可退出登录
- **登录被限流（后端是 5 次/分钟）时，登录按钮会禁用并倒计时 10 秒**，
  期间连按回车提交也会被拦下（细节见下文「接口被限流为什么有两种提示」）
- 登录弹窗的**「记住用户名」只把用户名写进 cookie**，密码不落任何客户端存储；
  老版本留下的、带着明文密码的那条 cookie 会在挂载时被自动改写（见下文「已知待办」）
- **接口出错时的提示带后端追踪号**（`X-Trace-Id`），用户报给站长即可快速定位；
  响应头里没有这个头时提示保持原样（细节见下文「错误提示为什么要带追踪号」）
- **每页都有 SEO 元信息**（title / description / og / canonical / `lang="zh-CN"`），
  后台页额外带 `noindex`，避免内部工具被搜到
- **受 429 限流时前端不会自动重试**：Nuxt 的 `$fetch`（ofetch）默认会对 GET 重试一次，
  而它的默认重试集合里包含 429 —— 也就是"后端正在限流，前端再打一次"，
  正好和限流的目的相反（细节见下文「为什么关掉 `$fetch` 对 429 的自动重试」）

### 几个刻意的实现细节

**首页为什么要服务端渲染（`index.vue`）**

改之前首页的数据是 `onMounted` 里发的请求 —— 那是**纯客户端渲染**，
服务端返回的 HTML 里**没有文章列表**：

- 搜索引擎抓到的首页是一张空壳，文章等于没被收录（对一个"要被搜到"的博客，
  这是硬伤；而文章详情页一直是 SSR 的，所以**详情页能被搜到、首页不能**）
- 首屏会先闪一下「加载中」再出内容

现在是 `useAsyncData`：服务端**等数据回来再渲染**，HTML 里直接带着文章列表，
并把结果写进 payload 让浏览器端复用（不会重复请求）。三条数据（文章列表 / 分类 /
站点统计）**并行**发起后一起 `await`，没有把首屏变成三次串行往返。

- **key 怎么定**：第一页是 `` `home-articles:${keyword}:${categoryId}:${tagId}` ``。
  key 是"这批数据是谁"的唯一标识，也是 payload 里的键，所以
  · **必须唯一**：不能和 `home-categories`、`home-tags`、`home-site-stats` 撞（撞了会共用同一份缓存）
  · **必须带上筛选条件**：写死成 `home-articles` 的话，"全部"与"分类 2"会被当成
    同一份数据 —— 换了筛选条件却显示上一批文章。**标签尤其不能漏**：
    分类和标签常常指向同一批文章，漏掉 tagId 时"标签 A"与"标签 B"会共用同一份
    payload 缓存，点了标签地址栏变了、列表还是上一批，而且不报任何错
  · 条件一变 key 就变，`useAsyncData` 自己会重新取，所以**不用再写一个 watch 去发请求**
    （多写一次等于同一批数据打两次后端，而 `/article/page` 有 300 次/分钟的限流）
- **筛选变化时只清"加载更多"的尾巴**：首屏那一页由新的 key 重新取，但
  `loadMore` 追加进来的还是旧条件下的文章，不清掉就会出现
  "新关键词的结果 + 老关键词的尾巴"这种混在一起的列表
- **「加载更多」仍然用普通请求**：它是在已有列表后面追加，不属于"这一页的首屏数据"，
  服务端也只渲染第一页，没必要进 payload
- **分类、标签与站点统计也一起改成 SSR**：分类与标签决定筛选条、也决定列表标题里
  那个名字（URL 里只有 `categoryId` / `tagId`，没有名字），留在 `onMounted` 里的话
  服务端渲染出来的标题永远是「最新文章」，带 `?tagId=` 的链接被抓到时看起来就是
  "筛选没生效"；统计留在 `onMounted` 里的话，SSR 的 HTML 上三个数字是
  **0**，而旁边已经渲染出了文章列表 —— "0 篇"和"旁边有文章"自相矛盾，
  正是本项目一直反对的"确定的错误答案"（失败时显示「—」，见上文）
- **服务端请求走内网地址**：复用已有的 `runtimeConfig.apiBaseServer`
  （`useApi` 的 `resolveApiBase()`），没有新造一套地址

**⚠️ 改成 SSR 之后测试必须卸载组件（这条坑记在这里，因为它只在测试里出现）**

`useAsyncData` 的结果会按 key 缓存进 Nuxt 的 payload，而**缓存的释放时机是组件卸载**。
测试环境里同一个文件的所有用例共用同一个 Nuxt 应用实例，不卸载的话下一个用例挂载时
`useAsyncData` 会发现"这个 key 已经有数据了"，于是**一个请求都不发**、直接沿用上一个
用例的数据 —— 表现是「分类接口失败」那条用例看到上一轮成功的分类、
或者「默认进页面」里 `/article/page` 一次都没被调用（断言连参数都拿不到）。
所以 `test/index.nuxt.spec.ts` 与 `test/homeContent.nuxt.spec.ts` 都加了
`enableAutoUnmount(afterEach)`（再 `await nextTick()` 等缓存清理真正落地）。
**现实应用里页面切走就会卸载**，所以这是让测试模拟真实生命周期，不是改生产代码绕开问题。

**全站 SEO 元信息（`app/utils/seo.ts` + `app/composables/useSeoMetaFor.ts`）**

改之前全站**只有一个 `<title>`，而且只有详情页设了**：没有 description、没有 og、
没有 canonical。后果都很具体：搜索引擎抓到的页面没有摘要（只能自己从正文里截，
常常截到导航文字或代码），链接分享到微信/群里没有卡片。

现在拆成两层，页面只描述"我是谁"：

| 层 | 文件 | 职责 |
|---|---|---|
| 纯函数 | `app/utils/seo.ts` | 标题/描述/canonical/og 的**拼装规则**、站点地址的归一化、兜底值 |
| 组合式函数 | `app/composables/useSeoMetaFor.ts` | 读运行时配置、把响应式数据接给 `useHead` |

- **页面怎么用**：`useSeoMetaFor(() => ({ path, title, description, type, image, noindex }))`。
  传**函数**是因为详情页在数据回来之前拿不到标题和封面，值必须能变
  （getter 里读到的数据一变，head 就跟着更新；首页的筛选条件同理）
- **每页都有**：`title` / `description` / `og:title` / `og:description` / `og:type` /
  `og:url` / `canonical` / `<html lang="zh-CN">`
- **`og:image` 只在有封面时出现**：拼成 `content: ''` 或 `undefined` 等于对外声明
  "这个页面有一张空图"，分享出去就是一张空白卡片 —— 而且只有真去分享时才看得出来。
  封面在库里存的是 `/uploads/x.png` 这种**相对路径**，而 og 规范要求绝对地址，
  所以统一由 `absoluteUrl()` 拼一次（已经是 `http(s)://` 的原样保留，以后挪到 CDN 也能用）
- **首页的 canonical 固定是不带参数的 `/`**：带 `?keyword=nuxt` 的搜索结果页与首页
  是同一份内容，直接说"我的正式地址只有 `/`"才不会让爬虫把每个关键词组合
  当成独立页面收录（重复内容）
- **后台页带 `noindex, nofollow`**：它是登录后才看得见的内部工具，被收录只有坏处。
  光靠 `robots.txt` 的 `Disallow` 不够：那只能挡住抓取，
  别处有链接时搜索结果里仍然可能出现这个地址（见下文 sitemap / robots 那一节）
- **站点地址 `siteUrl` 放在 `runtimeConfig.public`**（默认 `https://www.yigalaxy.xin`，
  可用 `NUXT_PUBLIC_SITE_URL` 覆盖）：canonical / og:url 必须是绝对地址，
  而同一份镜像要能在本地、测试域名、正式域名下跑。空值或
  `www.example.com` 这种缺协议的值会**回落到默认域名** —— 否则会拼出一个
  相对 canonical，搜索引擎直接忽略它，而页面看起来"填了"
- **SEO 这套东西全都在页面上看不出来**：title 拼错、canonical 写成相对地址、
  `og:image` 是空值，本地打开都正常 —— 所以规则由纯函数单测钉死（见测试章节）

**怎么验收（实测过，复现步骤）**

```bash
npm run build
# 后端不启动也要能渲染出带 meta 的 HTML（接口失败时列表是空的，但 head 必须完整）
NUXT_API_BASE_SERVER=http://127.0.0.1:59999 PORT=3111 node .output/server/index.mjs
curl -s http://127.0.0.1:3111/ | grep -o '<title>[^<]*</title>'
```

实测结果（后端**在跑**时）：`<title>亿轨星途 · 在代码与星轨之间，慢慢画自己的图。</title>`、
`<meta name="description" content="…">`、`<link rel="canonical" href="https://www.yigalaxy.xin/">`、
四条 `og:`、`<html lang="zh-CN">`，**文章卡片也在 HTML 里**（真实的两篇文章标题、
摘要、分类、日期、浏览量都能在 `curl` 的输出里看到），分类按钮同理。
后端**不在跑**时（`NUXT_API_BASE_SERVER` 指向一个死端口），
上面前四项一个不少，文章区渲染成「还没有发布任何文章」—— 接口失败不影响 meta 与整页渲染。

**应用外壳（`app.vue`）**

- **它才是真正的外壳**，不是每个页面各自搭一遍导航：导航栏、页脚、登录/注册弹窗、
  背景视频都在这里，页面只管内容区
- **刷新页面后能恢复登录态**：`useState('user')` 刷新后是 null，但 cookie 里的 token 还在，
  所以挂载时用 `GET /auth/me` 把用户信息补回来，顶部才能正确显示昵称和角色
- **sticky footer**：外壳是纵向 flex + `min-height: 100vh`，内容区 `flex: 1`，
  这样内容不足一屏时（后台页、文章页）页脚也贴在底部，不会在下面空出一片
- **背景视频在标签页切到后台时暂停**（监听 `visibilitychange`），
  避免看不见的时候还一直占用 CPU / GPU

**文章详情（`app/pages/article/[id].vue`）**

- **服务端渲染**：用 `useAsyncData` 而不是 `onMounted` + `ref`，
  服务端会**等数据回来再渲染**，所以 HTML 里直接带着正文 —— 这对 SEO 是决定性的
- **安全**：正文用 `md-editor-v3` 的**只读渲染组件 `MdPreview` 渲染，不是自己 `v-html`**。
  它内部接了 xss 白名单过滤，危险标签和属性会被清掉；
  自己拼 `v-html` 就是把用户/文章内容当代码执行
- **软 404**：文章不存在或未发布时 `throw createError({ statusCode: 404 })`，
  交给 `error.vue` 渲染成正常的错误页（而不是浏览器默认的报错页）
- 只读渲染组件比编辑器组件体积小得多，所以详情页不引入 `MdEditor`

**全局错误页（`error.vue`）**

- 按状态码给不同文案：404 / 403 / 401 / 500 各自说明"发生了什么、可以怎么办"，
  而不是统一一句"页面出错了"
- `useHead` 会把状态码写进页面标题（如「404 · 亿轨星途」）

**搜索、分类与标签筛选为什么要这么写（`useArticleFilter.ts` + `index.vue`）**

筛选条件有三个（关键词、分类、标签），但坑都在细节里：

- **关键词在输入框里和在请求里是两个变量**：`keywordInput`（输入框即时显示，打字不卡）
  与 `keyword`（生效中）。防抖只在"输入 → 生效"这一步做一次，请求、地址栏、标题
  全都读 `keyword`。
  第一版只把"写地址栏"防抖、请求却盯着输入框，结果**每敲一个字照样发一次请求** ——
  防抖等于没做（后端对 `/article/page` 还有 300 次/分钟的限流）。
- **标签并进同一个 composable，而不是另起一套**：分类、标签、关键词都是
  `/article/page` 上的过滤条件（后端按 AND 叠加），也都写在同一个地址栏里。
  给标签单独写一个 ref + watch + `router.replace`，就成了两套"谁负责写地址栏"的
  逻辑：点标签写一次、点分类又写一次，而两边读到的还是旧状态 ——
  最典型的表现是"点标签之后分类高亮莫名消失"。所以这里只**增加一个维度**，
  防抖 / 空值不写 / 后退回填这三条机制一条都没重写。
- **筛选条件写进地址栏，且用 `replace` 而不是 `push`**：搜索是连续微调的操作，
  每个中间状态都进历史记录的话，用户按一次后退只退回上一个关键词，想离开首页要按十几次。
- **双向同步**：浏览器前进/后退（或别处改了 query）也会把地址栏同步回状态，
  否则输入框和地址栏各说各话，用户会觉得"后退失灵"。同步时有一道
  「地址栏已经等于目标值就不写」的判断，否则会形成"后退 → 再往前写一次"的循环。
  **三个条件都要同步**：漏掉 tagId 的表现是"后退之后标签还高亮着，列表却已经是别的了"。
- **空值不写进地址栏**：不出现 `?keyword=&categoryId=&tagId=`，让"什么都没筛"只有一种表示。
- **点分类/标签时先把待生效的关键词落地**：用户"打完字还没停手就点了分类"时，
  条件要在同一次写入、同一次请求里生效，而不是先按旧关键词拉一遍、300ms 后再拉一遍。
- **标签没有再排一个「全部标签」按钮**：分类那一排只有一个「全部」，而标签的数量不定，
  再排一个按钮既占地方又和分类那排重复。所以取消的手势是**在同一颗胶囊上再点一次**
  （`selectTag` 收到同一个 id 就置空），一次性清干净仍然用「清除筛选」。
- **回到第 1 页**：筛选条件一变就重置页码。否则会出现"第 3 页 + 新关键词"这种
  后端并不存在的组合，用户只会看到空列表。
- **分类/标签拿不到就不显示那一条筛选**：接口失败、或者返回的不是数组时
  （`.find` 会直接抛错把首页渲染带崩），一律当成空列表 —— 一排空按钮比没有更糟。
- **列表标题要把范围拼完整**：三个条件可以叠加，标题只提其中一个会出现
  "同时选了分类和标签、标题只提分类"这种漏报，用户就不知道列表为什么少了几篇。

**文章标签（前台展示 + 按标签筛选 + 后台多选）**

标签是文章的第二套"归档"维度（分类是唯一的一个，标签可以有多个），前后端都是这一批新加的：

- **前台展示**：首页每张卡片在自己那篇文章下面列出它的标签（`#技术` 这样的实心小胶囊），
  详情页在标题上方列出（和分类同一行）。后端列表项与详情都带 `tags`，
  **没有标签时是空数组**（不是 null），前端仍然用 `Array.isArray` 兜一道 ——
  为了几颗标签把正文页渲染带崩是绝对不能接受的。
- **点标签就是筛选**：卡片上的标签、详情页的标签、以及首页那排标签筛选条，
  三个入口最后都落到 `useArticleFilter.selectTag(id)` 这一个函数上，
  写入同一个 `?tagId=`、共用同一次 `/article/page` 请求。
  - 卡片上的胶囊必须 `@click.stop.prevent`：`.stop` 拦掉冒泡，否则点标签会变成
    "打开当前这篇文章"（卡片外层是 `<a @click.prevent="goArticle">`）
  - 详情页的标签写成真实的 `<a href="/?tagId=3">`（配合 `.prevent` 走站内跳转）：
    内链是爬虫发现"这个标签下还有哪些文章"的入口，中键/右键新标签打开也照常可用
- **筛选条与分类那排是两个类名**（`.cat` / `.tags .tagp`）：首页"内容真实性"那组用例
  是按 `.cat` 数分类个数的，两边共用类名会让那个断言跟着标签数量一起变 —— 一种假绿。
- **标签上的文章数放进了 `title` 提示**：后端给了 `articleCount`（**只统计已发布**，
  没有文章时是 `0` 而不是 null），但把它印在胶囊上会把导航条撑得很宽、
  还容易让人以为那是个可以点的数字。悬停一句"该标签下有 3 篇已发布文章"更合适；
  数字本身在后台的标签管理列表里是正经的一列。
- **后台文章弹窗的标签多选**：选项来自 `GET /admin/tag/list`，打开弹窗时**重新拉一次**
  （用户很可能刚在标签管理里建完标签就来写文章）。回显**以文章详情为准**——
  列表接口里那份 `tags` 可能是几分钟前拉的，拿它回显会把别人刚改过的标签覆盖掉；
  详情接口挂了时才退回用列表那份，**绝不能显示成"一个都没选"**（那会让用户一保存就清空标签）。
- **列表标题把范围拼完整**：`「关键词」在「分类」分类与「标签」标签下的结果` ——
  三个条件可以叠加，标题只提其中一个就会出现"同时选了分类和标签、标题只提分类"这种漏报。

**联调实测（后端跑在 `http://localhost:8082`，未重启过它）**：

| 步骤 | 请求 | 响应 |
|---|---|---|
| 建标签（名字前后**故意带空格**） | `POST /admin/tag` body `{"name":"  联调标签A  ","sort":1}` | `{"code":200,…, "data":3}`，列表里回来的是 `"name":"联调标签A"`（后端 trim 后落库） |
| 建第二个 | `POST /admin/tag` body `{"name":"联调标签B","sort":2}` | `data: 4` |
| 重名（去空格后同名） | 再 POST 一次 `联调标签B` | `{"code":400,"message":"标签名已存在"}`，**HTTP 仍是 200** —— 前端按 `code` 判断，不能只看状态码 |
| 打标签 | `PUT /admin/article/117` body 带 `"tagIds":[3]` | `{"code":200}`；`GET /article/117` 的 `tags` 变成 `[{"id":3,"name":"联调标签A",…}]` |
| **按标签筛** | `GET /article/page?tagId=3` | `total: 1`，且 `records[0].id = 117` —— 真的只返回打了这个标签的那一篇 |
| 空标签 | `GET /article/page?tagId=4`（标签 B 还没打给任何文章） | `total: 0`，`records: []`（不是报错） |
| 叠加筛选 | `GET /article/page?tagId=3&categoryId=1` / `&categoryId=2` | `total: 1` / `total: 0` —— 两个条件确实是 AND 叠加 |
| 前台公开接口 | `GET /tag/list` | 和后台那份形状一致，`articleCount` 都是 `0`（此时两枚标签下都没有已发布文章） |

> ⚠️ 联调时用 `PUT /admin/article/117` 打标签**覆盖掉了那篇测试文章的正文**
> （后端 update 是显式 SET，没传的字段会被写 null）——已经用 binlog 的行镜像
> （before-image 里同时存着修改前的正文）把原来的标题/正文/摘要还原回去了，
> 只保留联调打上的那枚标签。教训：**在真实数据上发全量 PUT 之前，先把原值读出来原样带回去。**

**后台的标签管理与只读分类（`admin.vue`）**

原来的「分类 / 标签」是一个写死"该模块开发中"的占位菜单。这次拆成两个真实页面：

| 菜单 | 状态 | 说明 |
|---|---|---|
| 标签管理 | **可用**（增 / 改 / 删） | 后端有完整的 `/admin/tag` 写接口，所以这里是真能用的，不是占位 |
| 分类 | **只读** | 后端其实**已经有**分类的增删改（`AdminCategoryController`），但这一批任务的约定是"分类的写操作不在范围内"，所以只接了列表 |

- **为什么不给分类摆一个"该模块开发中"的占位**：分类数据是真实存在、真实可读的，
  摆占位等于把已经有了的东西说成没有。只读列表至少是诚实的 ——
  管理员能看见现在有哪些分类、各是什么描述，页面顶部也写明了"只读，接口有、前台没接"，
  免得有人以为是后端不支持。**也绝不摆一个点了没反应的"新建分类"按钮**：
  那比没有更糟（这一条有用例守着：分类页上不允许出现任何"新建/编辑/删除/保存"字样的按钮）。
- **两个菜单而不是一个**：它们背后是两套不同的能力，挤在一个入口里没法表达
  "标签能改、分类暂时不能"。
- **标签列表和文章弹窗的下拉框共用同一个 `tags` ref**：管理员在这里建完标签，
  切到文章管理打开弹窗就能选到它，不需要"刷新整个页面"。
  切到标签菜单时会重新拉一次（在别处改过标签时看到的才是当前的）。
- **保存失败时弹窗不关、原因写在弹窗里**：最典型的失败是重名（后端 400「标签名已存在」）。
  `useApi` 已经弹过一次 toast，但 toast 三秒后就消失，而用户此刻正在看弹窗、
  接下来要做的动作是"改个名字再点一次保存" —— 所以原因必须留在弹窗里。
- **提交前一定 trim**：后端也是 trim 之后才查重（"工作" 与 " 工作 " 是同一个名字）。
  不 trim 的话请求体和日志里都是脏数据，而且 `maxlength=30` 会因为有空格而提前截断
  一个本来合法的名字。
- **删除的二次确认必须说清影响**：后端这个删除是**物理删除**，
  同时把它与文章的所有关联一起删掉（文章本身不删）。
  确认框里会报出"它下面还有 N 篇已发布文章"（`articleCount` 就是为这个准备的），
  并写明"删除后无法恢复" —— 不写清楚的话，用户很容易以为只是从标签库里去掉了。
- **删除撞上 404 要能自愈**：别人（或另一个标签页）已经把它删掉时，
  只说一句"标签不存在"的话，用户看到的是"报错 + 表格里还列着它"，
  会以为删除失败再点一次。现在的做法是提示"这个标签已经不在了，列表已刷新"并真的重拉一次。
- **「已发布文章数」的 0 就显示 0**：它是后端一条 `GROUP BY` 算出来的确定答案
  （没有文章就是 0，不是"读不到"），显示成「—」反而会让人以为接口坏了 ——
  这跟首页统计失败时显示「—」是两个相反的场景，别混。

**联调实测（后端跑在 `http://localhost:8082`，未重启过它）**：

| 操作 | 请求 | 响应 |
|---|---|---|
| 新建 | `POST /admin/tag` `{"name":"联调标签C","sort":9}` | `{"code":200,"data":5}` |
| 改名 | `PUT /admin/tag/5` `{"name":"联调标签C改","sort":9}` | `{"code":200}`；列表里第 5 项已经变成 `联调标签C改` |
| 删除 | `DELETE /admin/tag/5` | `{"code":200}`；列表回到只剩两枚标签 |
| 重复删除 | 再 `DELETE /admin/tag/5` | `{"code":404,"message":"标签不存在"}` —— 这正是那句"列表已刷新"提示要处理的场景 |
| 名字超长 | `POST` 31 个字母 | `{"code":400,"message":"标签名最长 30 字"}` |
| 名字全是空格 | `POST` `{"name":"   "}` | `{"code":400,"message":"标签名不能为空"}` |
| 已发布文章数 | `GET /admin/tag/list` | 打给文章 117 的那枚标签是 `"articleCount":1`，另两枚是 `0` |
| 传不存在的标签 id | `PUT /admin/article/117` 带 `"tagIds":[999999]` | `{"code":404,"message":"有标签已不存在，请刷新页面后重试"}`，**且 117 原有的标签没被改动**（再次 GET，`tags` 仍是那枚 `联调标签A`）—— 后端的"校验先于写入"实测成立 |

**文章评论区（`app/pages/article/[id].vue` + `app/utils/comment.ts`）**

评论区是这个站点的"对话"部分，也是**第一个允许游客往数据库写东西**的入口，
所以它比别的模块多两层考虑：写进去的内容安全吗？用户知不知道自己到底发成功没有？

- **数据获取用 `useAsyncData`（服务端渲染）**，key 是 `article-comments:<文章id>`：
  - 理由与正文相同：评论是**内容**，出现在服务端 HTML 里才不会先闪一下"加载中"
  - **代价写在明处**：评论请求并进了详情页 SSR 的关键路径（所以和正文**并行**发起，
    串行 await 会让首屏多等一个往返）、payload 里多了一份评论数据（第一页 10 条）
  - 取消这份代价的做法（纯客户端 `onMounted`）也考虑过：那样首屏 HTML 里没有评论、
    接口挂了也不会影响任何渲染，但会把"评论区先空一下再出现"变成固定表现 ——
    对一个内容站来说不划算
  - **失败绝不连累正文**：`normalizeCommentPage()` 把任何失败整成"空列表 + 总数 0"，
    页面最多显示"还没有评论"，正文照常渲染（有用例守着）
  - **key 必须带文章 id**：写死成 `article-comments` 的话，从第 12 篇点到第 13 篇
    会拿到上一篇的评论 —— 两篇都有评论，只是内容不对，界面上完全看不出来
- **分页用「加载更多」而不是页码器**：和首页保持一致，而且评论是对话，
  追加到末尾符合阅读顺序；页码器会把用户"跳"到另一页、丢掉正在读的位置
  - 下一页的页码是 `ceil(已加载条数 / 每页条数) + 1`。
    **不能写成 `floor(...) + 1`**：第一页只返回 2 条（页大小 10）时，
    `floor(2/10)+1 = 1` —— 又把第 1 页拉了一遍，同两条评论在页面上出现两遍。
    这个 bug 只在"某一页不满一整页"时出现，而最后一页永远不满一整页
- **发表表单的校验与后端一致**，且规则抽在 `app/utils/comment.ts`（纯函数，30 个用例）：
  - 昵称必填 ≤50、内容必填 ≤1000、邮箱选填；
  - **邮箱校验故意比后端松**（只要有 `@`、两侧非空、无空白就算过）：
    后端用的是 Jakarta `@Email`，规则很宽；前端如果写一个更严的正则，
    就会把后端本来接受的地址挡在门外 —— 而用户**没有任何办法绕过前端**，
    只能换邮箱或者放弃评论。前端更松最坏只是"多一次往返 + 后端回一句
    「邮箱格式不正确」"，用户看得见也改得动。**权威校验交给后端**
  - **邮箱留空时把整个字段去掉**，而不是发一个空串：那样库里会存 `""`，
    而"没填"在数据上应该是 `null`
- **提交成功后必须说清"在等审核"**：后端默认把评论存成待审核（`status=0`），
  所以它**不会**立刻出现在上面的公开列表里。只清空输入框、什么都不说的话，
  用户看到的是"我发的评论没了"，然后很可能再发一遍 ——
  同一条评论就进了两次审核队列。所以三件事一起做：
  ① 一句「评论已提交，等待审核」 ② 把刚提交的那条显示出来
  ③ 挂在「你的评论（仅你可见）」那一块并标上「待审核」，说明它为什么不在上面的列表里
  - 这个提示用的是**提交接口返回的那条数据**（它带着 `status:0`），**不是**刷新列表 ——
    刷新也看不到它（公开列表接口只返回已通过的）
  - 提交成功后**只清内容、保留昵称与邮箱**：同一个人常常连着说几句
- **内容按纯文本渲染，绝对不用 `v-html`**（这一页最要紧的一条）：
  - 后端在入库前已经把 `<` `>` `&` 转义成实体，所以前端**照原样显示**就是安全的：
    用 `{{ }}` 插值 = 把字符串当纯文本放进 DOM，内容里写 script 标签也只会显示成一串字
  - 两条配套的纪律：**不要反转义**（那正好把后端那层保护拆掉）、
    **也不要再转义一次**（会把 `&lt;` 显示成 `&amp;lt;`，用户看到一堆实体码）
  - 用例用两种输入钉住：后端转义过的内容（断言显示的就是实体码本身）、
    以及**万一后端漏了转义返回原生标签**的情况（断言页面上不会多出元素）——
    后者是"就算后端出问题、前端也不执行"的兜底
- **限流（20 次/分钟的整站配额）必须提示成"请求过于频繁"**，不能显示成"网络异常"：
  这个接口的额度很小、很容易撞到，而限流是"服务端好好的、只是让你等一会儿"，
  说成网络故障会把人往"查网线、重启路由器"的方向带，而且每重试一次还会再吃掉一个名额。
  文案直接复用 `app/utils/apiError.ts` 的 `RATE_LIMITED_MESSAGE`（后端给了原文就用后端的）
- **提交中禁用按钮**：评论接口有配额，多打一次就少一次；而且按钮上的 `:disabled`
  在"两次点击落在同一帧"时还没重绘，所以真正的拦截放在发请求之前那一层
- **提交失败不清空用户写的内容**：限流/网络抖动都可能让人白写一段话

**联调实测（后端跑在 `http://localhost:8082`，未重启过它）—— 完整链路**：

| 步骤 | 请求 | 结果 |
|---|---|---|
| ① 发表 | `POST /comment` `{articleId:117,nickname:"联调读者",email:"reader@example.com",content:"联调用的一条评论 <script>alert(1)</script> & 结束"}` | `{"code":200,"data":{"id":2,…,"status":0,"createTime":null}}`，内容回来时已经是 **`&lt;script&gt;alert(1)&lt;/script&gt; &amp; 结束`**（后端转义） |
| ② 前台立刻看 | `GET /comment/list?articleId=117` | `records: []`、`total: 0` —— **待审核的评论对前台不可见**，这就是那句"等待审核"的由来 |
| ③ 不带 articleId | `GET /comment/list?page=1&size=10` | `{"code":400,"message":"articleId 不能为空"}`（HTTP 200 + code） |
| ④ 后台看待审核 | `GET /admin/comment/page?status=0` | 能看到它，并且**多出 `articleTitle:"测试"`、`email`、`ip:"0:0:0:0:0:0:0:1"`** 三个前台没有的字段 |
| ⑤ 审核通过 | `PUT /admin/comment/2/status?status=1` | `{"code":200}` |
| ⑥ 前台再看 | `GET /comment/list?articleId=117` | `records` 里出现了它，`status:1`，内容仍是转义后的实体 —— **前台可见了** |
| ⑦ 隐私边界 | 前台那份响应体里搜 `email` / `ip` | **0 处命中** —— 两个 VO 的差别实测成立 |
| ⑧ 限流 | 连发 22 次 `POST /comment`（`articleId:999999`，文章不存在 → 业务上 404、**一个字节都没写库**） | 前 20 次都是 `HTTP 200 + {"code":404,"message":"文章不存在"}`，**第 21、22 次变成 `HTTP 429 + {"code":429,"message":"请求过于频繁，请稍后再试"}`** —— 真·HTTP 429，前端由此走 `apiError` 的限流分支而不是"网络异常" |

> ⑧ 这个探法值得记下来：**用"业务上必然失败"的请求去压限流额度**，
> 既能确认限流真的生效、又不会往库里写进 22 条垃圾评论。
> 顺带实测出一个后端行为：限流是**在业务校验之前**就扣额度的
> （那 20 次 404 照样消耗了配额）。

**后台的评论审核（`admin.vue`）**

**这个页面不是"锦上添花的后台功能"，而是评论模块能不能真正跑起来的最后一环**：
评论默认是待审核的，不点通过，前台**永远**看不到它 —— 前台那句"评论已提交，等待审核"
就是靠这里兑现的。没有它，别人写的评论只有作者自己看得见。

- **默认筛选是「待审核」**：这个页面 90% 的用途是"把攒下的待办处理掉"。
  默认「全部」的话，新评论会淹没在已处理过的评论里，每进一次页面都要先手动选一次筛选。
- **菜单上有待审核数量角标**：进后台就该看见"有几条要处理"，而不是必须点进菜单才知道。
  数量为 0 时整个角标不渲染（显示一个 0 只是噪音）。
  - **角标和列表的 total 不是一回事**：total 是"当前筛选条件下的条数"。
    在「已通过」里拒绝一条，列表 total 不变、待审核却 +1 ——
    所以那种情况下要**额外查一次**待审核数（当前视图本来就是待审核时则直接复用 total，省一次请求）
- **通过 / 拒绝不做二次确认，只有删除做**：
  - 审核是**可逆**的：拒绝掉的评论还在「已拒绝」里，随时能再通过。
    给可逆操作加确认框，只会让"处理一批待审核"变成一道道弹窗
  - **删除不可逆**（后端是逻辑删除，删完前台后台都不显示、界面上没有恢复入口），
    所以它要确认，并且确认文案里写明"前台与后台都不再显示、无法恢复" ——
    只说"删除评论"的话，管理员不一定知道前台也看不到了
- **已经是当前状态的那个按钮禁用**：点它不会发生任何事，留着可点反而让人以为"点了没生效"
- **状态筛选走查询参数**（`PUT /admin/comment/{id}/status?status=1`）：
  后端签名是 `@RequestParam Integer status`，塞进请求体里后端收不到、直接 400。
  用例专门断言了这一点（`params` 而不是 `body`）
- **「全部」用一个界面专用的哨兵值 `-1`，而且不发这个参数**：
  后端的状态只有 0/1/2，`-1` 只表示"不按状态过滤"。
  【为什么不用更自然的 `null`】`el-option` 的 value 类型不含 Null，
  传 null 会在**每次渲染**打一条 `Invalid prop: type check failed for prop value` ——
  不影响功能，但会把控制台与 CI 日志刷屏，真正的报错混在里面就被忽略了
  （和已经修掉的 `el-link` 废弃警告是同一类问题，修法也一样：换写法 + 用断言钉住）
- **404 能自愈**：评论可能已经被别人（或另一个标签页）删掉了。
  这时只报一句"评论不存在"的话，用户看到的是"报错 + 表格里还列着它"，
  会以为操作失败再点一次。现在会提示"这条评论已经不在了，列表已刷新"并**真的重拉一次**
- **评论内容同样按纯文本渲染**（`{{ }}`，不是 `v-html`）：后台没有理由比前台更宽松
- 文章的标题可能已经是 null（文章被删了、评论还在），这时"文章"列显示"（文章已删除）"
  而不是一个空白单元格 —— 空白会让人以为是页面没加载出来

**联调实测（后端跑在 `http://localhost:8082`，未重启过它）—— 待审核 → 通过 → 前台可见 → 删除 → 前台不可见**：

| 步骤 | 请求 | 结果 |
|---|---|---|
| ① 发表 | `POST /comment` | `{"code":200,"data":{"id":3,"status":0,"createTime":null}}` —— 新评论是**待审核** |
| ② 后台待办 | `GET /admin/comment/page?status=0&page=1&size=1` | `total: 1`（菜单角标读的就是这个数） |
| ③ 前台 | `GET /comment/list?articleId=117` | `records` 里**没有**它 —— 待审核对前台不可见 |
| ④ 非法状态 | `PUT /admin/comment/3/status?status=0` / `?status=3` | 都是 `{"code":400,"message":"状态只能是 1(通过) 或 2(拒绝)"}` —— 审核只有通过与拒绝两个结果 |
| ⑤ 通过 | `PUT /admin/comment/3/status?status=1` | `{"code":200}` |
| ⑥ 前台 | `GET /comment/list?articleId=117` | `total: 2`，两条都带 `status:1` —— **前台可见了** |
| ⑦ 删除 | `DELETE /admin/comment/3` | `{"code":200}` |
| ⑧ 前台 | `GET /comment/list?articleId=117` | `total: 1` —— **又看不到了** |
| ⑨ 再删一次 | `DELETE /admin/comment/3` | `{"code":404,"message":"评论不存在"}` —— 正是"列表已刷新"那句提示要处理的场景 |

**另外用生产构建做了一次真正的 SSR 实测**（`npm run build` 之后起 `.output/server/index.mjs`，
`NUXT_API_BASE_SERVER` 指向跑着的后端，然后 `curl /article/117`）：

- 服务端 HTML 里**直接带着评论**：`<p class="cm-body">联调用的一条评论 &amp;lt;script&amp;gt;...`
  —— 也就是说浏览器会把这段显示成 `&lt;script&gt;alert(1)&lt;/script&gt;` 这样的**纯文本**，
  页面上不会多出任何 script 元素。这同时印证了两件事：① 评论是 SSR 的（爬虫与首屏都拿得到）
  ② **前端绝不能反转义**：一旦把 `&lt;` 还原成 `<` 再插值，这段就变成真正的标签了
- 同一份 HTML 里文章标签也在：`<a class="doc-tag" href="/?tagId=3" title="查看「联调标签A」标签下的全部文章">#联调标签A</a>`
- 评论数 <code>1</code>、发表表单、空状态互斥关系都对（有评论时不出现"还没有评论"）

> ⚠️ **联调中发现的后端问题：转义的范围比契约里写的更宽**
> 契约说"内容里的 `<` `>` `&` 已被转义"，但实测把一段带箭头符号的评论发上去，
> 回来的是 `待审核 &rarr; 通过 &rarr; 前台可见` —— **`→` 被转成了 `&rarr;`**。
> 后端用的是 Spring 的 `HtmlUtils.htmlEscape`，它除了那几个 ASCII 字符，
> 还会把一批非 ASCII 符号换成 HTML4 具名实体（`—`→`&mdash;`、`…`→`&hellip;`、`©`→`&copy;` 等）。
> **后果**：这些符号在页面上会**原样显示成 `&rarr;` 这样的码**，而不是 `→`。
> 前端**不能**为了好看去反转义（那会把 XSS 防护一起拆掉），所以这件事应该在后端修：
> 只转义 `< > & " '` 五个字符（Spring 的 `HtmlUtils.htmlEscape` 默认其实只处理这几个，
> 这里的行为说明转义的实现比预期更激进），或者改成"存原文 + 渲染时转义"。
> 已记入「已知待办」，并**逐字写清了实测到的输入与输出**。

**站点统计为什么必须走后端接口（`useSiteStats.ts`）**

首页个人卡片与后台概览的数字都来自 `GET /article/stats`，而不是前端自己算：

- **口径只能有一个地方定**：改之前首页的文章数是 `/article/page` 的 `total`
  （加了筛选就变成"符合条件的篇数"）、浏览量是已加载文章的 `reduce` 求和
  （点一次「加载更多」数字就变）、分类数是当前页列表长度 —— 三个数字三种口径。
  现在合并成后端一条聚合 SQL，前端只负责显示；以后要改口径也是改后端。
- **成本更低**：前端求和得先把文章全拉回来；聚合查询只回一行。
- **失败必须降级**：这三个数字出现在首屏，接口挂掉绝不能连累整页渲染。
  `useSiteStats` 不抛异常，失败时把数字留成 0（或上一回的值）、把 `failed` 立起来，
  由页面显示「—」。**为什么不用 0 代替**：0 是一个"确定的答案"，
  访客会以为站点真的没有文章；「—」才是诚实的"暂时读不到"。
- **失败时保留旧数字**：有上一回成功的数据时，宁可真值 + 占位，也不要清零。
- **后台概览的用户数是独立的一次 `page=1&size=1` 查询**，
  不复用用户表格的 `total` —— 那个会被管理员在用户管理页设的筛选条件影响，
  而概览要的是"全站有多少用户"。`size=1` 是因为只要 `total` 这个总数，
  不需要真把那一页数据拉回来。
- **进页面就主动加载**：概览的数字不再依赖"你点开过哪些菜单"；
  切到「概览」菜单时再刷新一次，这样刚发布完文章回到概览看到的是新数字。

**首页为什么把这些东西删掉（`index.vue`）**

原则是那句老话：**要么接真实数据，要么不显示，不留"看起来像真的"的假内容。**
假内容最麻烦的地方不是它假，而是它和真数据混在一起，看的人分不出来 ——
下面这几处之前都藏在"要点一下才看得见"的地方，翻 HTML 都不一定发现：

| 删掉的东西 | 假在哪 | 为什么不接真数据 |
|---|---|---|
| 侧边「后台留言」浮窗（访客A / 访客B 两条留言） | 写死的数组，不是评论 | 后端没有评论模块，前端拿不到任何评论数据，只能删 |
| 左下角「1 人正在看」 | 写死的字符串 | 后端没有在线人数接口；这个数字永远不会变，连"当前访客自己"都不一定算得准 |
| 音乐卡片的三个曲名与上一首/下一首按钮 | 曲名是编的，音频只有一个文件 | 没有播放列表接口；点下一首只是把下标加一、曲名换个字，声音重新放同一段 |
| GitHub / 邮箱 / RSS 三个按钮 | 背后没有任何地址，点了没反应 | 没有真实链接；本站也没有 RSS 输出（后端没有这个接口） |

处理方式不是 `v-if="false"` 藏起来，而是连**面板、按钮、样式、以及只被它们用到的
变量**一起删掉（`tracks` / `cur` / `prev` / `next` / `resetAudio` / `msgs` /
`open.msg` / `pos.msg` 与对应的 CSS 都清掉了）。留着"只是没启用"的代码，
下次有人看见就会以为它是个待接的功能。

音乐卡片保留下来，但只保留真实存在的部分：**一个音源**（`static-media/bg-music.mp3`，
页面上引用为 `/media/bg-music.mp3`）、
**一个播放键**、进度条，标题写「背景音乐」而不编一个曲名。
它也仍然是导航栏「音乐」指向的 `#music` 锚点，所以不能整块删掉。
（同一张卡片上方那三个统计数字在接口失败时显示「—」而不是 0，见上一节。）

首页**公告跑马灯**是唯一保留的写死文案：它是站点自己的宣传语（和页脚、
个人简介同一类），不是伪装成接口数据的东西，所以在接后端公告模块之前先维持原样。
这些"删掉的东西不会再回来"由 `test/homeContent.nuxt.spec.ts` 的 11 个用例守着。

### 归档页（`app/pages/archive.vue`）

首页是**信息流**：它擅长"看最近有什么"，不擅长"找去年 3 月那篇"。
归档页把同一批文章按时间轴重新排一遍 —— 一屏扫完，这是博客该有的第二个入口。

**数据来自后端 `GET /article/archive`**（公开接口，走缓存），形状是
`{ months: [{ year, month, count, articles: [{ id, title, createTime }] }], total }`：
`months` 已经**按时间倒序**、每个月里的 `articles` 也**已倒序**，只含已发布文章（最多 500 篇）。

**为什么前端一次都不排序** —— 这是这一页最要紧的一条纪律：

- 后端已经排好了，前端再排一次等于把同一条排序规则维护成两份。
  哪天后端改了（加置顶、或改成按更新时间），前端这份就会把它**整个盖掉**
- 而症状极其隐蔽：页面上看不出任何异常，只是顺序不对。
  没有用户会因为"9 月的文章排在 10 月前面"来提 bug，只会觉得"这个站有点怪"
- 所以用例里**故意喂乱序的输入**（`2026-1 / 2025-12 / 2026-9`），
  断言页面输出与输入逐字一致 —— 用"看起来已经有序"的数据是抓不到这个 bug 的

**为什么整页平铺，而不是用 `el-collapse` 折叠**：

| | 平铺（现在的做法） | 折叠 |
|---|---|---|
| "一眼看全部" | 进来就是全部 | 默认收起，得一个月一个月点开 —— 而"一眼看全部"正是这一页存在的理由 |
| 浏览器 `Ctrl+F` | 能找到任何一篇 | **收起的面板里的文字找不到**（Element Plus 的折叠不渲染收起的内容）。归档页恰恰是"我记得标题里有个词"的地方 |
| 高度控制 | 不需要：每一条只有"日 + 标题"一行，本身足够紧凑 | 需要折叠的场景是"每条都很高"，这里不成立 |
| 实现成本 | 一个 `v-for` | 还得给每个面板传默认展开的 name 数组 —— 用了折叠却全是展开态，多一层交互和 DOM 却换不到任何东西 |

**降级绝不变成错误页**：接口挂了 / 被限流 / 返回结构变了时，
`months` 兜成空数组、页面显示一句「归档暂时读不到，请稍后再试」+ 一个回首页的入口。
不抛 `createError` 的理由：归档只是"换个角度看文章"，它挂了不该把整站点变成错误页 ——
首页、详情页那些入口都还是好的。另外，**空数据与读不到必须是两句不同的话**：
前者是"还没有发布任何文章"（让人去写第一篇），后者是"稍后再来"。
写成同一句会把用户指到错的方向。

**几个小的显示取舍**：每条只印"日"（年月已经在段标题里，再印一遍是重复信息还会把标题挤走）；
`count` 优先用后端给的（前端数 `articles.length` 只是"这一页渲染了几条"，口径不同），
缺失时才回落到数组长度；`total` 与 `count` 都过 `toCount()`，
不会在页面上出现 `NaN` 或 `-1 篇`；`createTime` 用**正则**取日而不是 `new Date()` ——
后端的 `LocalDateTime` 没有时区，交给 `Date` 会被按浏览器本地时区掰一次，凌晨那几篇会差一天。

**顺带做的一件事**：`/archive` 加进了 `server/utils/sitemap.ts` 的 `STATIC_PAGES`。
它是一个导航里点得到的独立入口，漏掉它不会有任何报错 ——
表现只是"爬虫得先发现导航里的链接才知道有这一页"，而站点地图的意义正是"不用等链接被发现"。

**联调实测（后端跑在 `http://localhost:8082`，全程没有重启过它）**：

| 步骤 | 请求 | 结果 |
|---|---|---|
| ① 真实接口 | `GET /article/archive` | `{"code":200,…, "data":{"months":[{"year":2026,"month":9,"count":2,"articles":[{"id":117,"title":"测试","createTime":"2026-09-10T05:03:19"},{"id":116,"title":"你好，亿轨星途","createTime":"2026-09-10T04:49:43"}]}],"total":2}}` |
| ② 生产构建 + SSR | `npm run build` → `NUXT_API_BASE_SERVER=http://localhost:8082 PORT=3111 node .output/server/index.mjs` → `curl -s http://127.0.0.1:3111/archive` | **200**，HTML 里直接带着这两篇：`<p class="ar-sub">按年月浏览全部已发布文章 · 共 2 篇<span>，1 个月</span></p>`、`<span class="am-date">2026 年 9 月</span><span class="am-count">2 篇</span>`、两条 `<a href="/article/117">…<span class="am-day">10</span><span class="am-title">测试</span>` / `href="/article/116"` |
| ③ head | 同一份 HTML | `<title>归档 · 亿轨星途</title>`、`<meta name="description" content="亿轨星途的全部已发布文章，按年月倒序排列，可以按时间线找到任意一篇旧文。">`、`<link rel="canonical" href="https://www.yigalaxy.xin/archive">` |
| ④ 导航入口 | 同一份 HTML | `<a aria-current="page" href="/archive" class="router-link-active router-link-exact-active nv">归档</a>` |
| ⑤ 站点地图 | `curl -s http://127.0.0.1:3111/sitemap.xml` | 静态页里多了 `<loc>https://www.yigalaxy.xin/archive</loc>`（`changefreq: daily` / `priority: 0.9`），后面跟着两篇真实文章 |

### sitemap.xml 与 robots.txt（Nitro 运行时路由）


要能被搜到，除了页面上的 meta 还需要这两份文件：爬虫靠 `robots.txt` 知道
"什么可以抓、sitemap 在哪"，靠 `sitemap.xml` 知道"站点上有哪些地址"。
两者都由 **Nitro 的运行时路由**提供（`server/routes/*.get.ts`），
内容生成在 `server/utils/sitemap.ts`（纯函数，有单测）。

**为什么是运行时路由，而不是构建时生成一个静态 `sitemap.xml`** —— 两个硬约束：

| 构建时生成 | 运行时生成（现在的做法） |
|---|---|
| 前端镜像的 `docker build` 里**连不上后端**（构建阶段后端容器可能还没起、也不在同一个网络），只能构建失败或生成一份**空的** sitemap —— 后者更糟：构建看着成功了，而线上那份 sitemap 永远只有首页 | 构建与后端解耦，构建阶段只编译代码 |
| 新文章发布后必须**重新构建、重新部署镜像** sitemap 才更新，而"发文章"和"重新构镜像"在时间上毫无关系，站长很容易漏掉这一步 | 发布后最多等一个缓存周期（`Cache-Control: max-age=300`）就更新 |

**"取全部文章"必须分页循环**：后端 `GET /article/page` 单页上限是
**50 条**（`ArticleQuery.MAX_PAGE_SIZE`，传更大的 size 会被夹到 50），
所以只取第一页的话，第 51 篇之后的文章**永远不会**出现在 sitemap 里。
更麻烦的是这件事**在文章少的时候完全看不出来**（几十篇时一页就取完了），
等文章多起来才发现"最近的文章都没被收录"，而那时已经过去很久。

三种终止条件缺一不可（都在 `collectAllArticles()` 里）：

- 这一页返回空数组 → 没有更多了（也兜住了后端 `total` 不准的情况）
- 已取条数 ≥ `total` → 取完了（`total` 为 0 / 缺失时不据此判断）
- 翻到 `MAX_ARTICLE_PAGES`（40 页）→ **防死循环的上限**。没有它，
  一个错误的 `total` 会让循环一直翻下去，表现是"sitemap 请求挂着、把后端打满"

**接口挂了也要返回一份合法的 sitemap**（降级，绝不 500、绝不空 body）：

- 爬虫收到 500 会反复重试、收到**空 body** 会当成非法 sitemap；
  而"暂时只收录首页"只是少收录，代价小得多
- 已经取到的那部分照样写进去（第一页拿到的 50 篇是真数据，没理由丢掉）
- 一份"只有首页"的 sitemap 与正常版本长得很像 —— 结构、命名空间、
  `Content-Type` 全部一致，区别只是少了几条 `<url>`
- 实测：把 `NUXT_API_BASE_SERVER` 指向一个死端口之后，
  `/sitemap.xml` 仍然返回 **200 + `application/xml`**，内容是一份只含首页的合法 urlset

**robots.txt**：允许抓取（`User-Agent: *` + `Allow: /`）、
挡掉后台（`Disallow: /admin`）、并用**绝对地址**指向 sitemap
（`Sitemap: https://你的域名/sitemap.xml` —— 写成 `/sitemap.xml` 会被直接忽略，
而且没有任何提示）。

- **它从 `public/robots.txt` 改成了运行时路由**：原来那个静态文件只有两行，
  而且**没法带域名**（静态文件里写死域名的话，本地 / 测试 / 正式环境就各需要一份），
  于是 `Sitemap:` 这一行根本没法正确写出来。
  现在域名从 `runtimeConfig.public.siteUrl` 读，三处地址（canonical、sitemap 里的
  `<loc>`、robots 里的 `Sitemap:`）由**同一个** `absoluteUrl()` 拼出来 ——
  各写一份的话迟早会出现"canonical 指向 A 域名、sitemap 指向 B 域名"，
  而两份文件单独看都没问题
- **后台为什么既 `Disallow` 又 `noindex`**：两者挡的不是一回事。
  `Disallow` 只挡住"抓取这个地址"，但别处有链接指向它时，搜索结果里
  仍然可能出现这个地址（只是没有摘要）；`noindex` 才是"别收录"

**⚠️ 部署注意**：这两条是**运行时路由**，不是 `.output/public` 里的静态文件 ——
Nginx 必须把 `/robots.txt` 与 `/sitemap.xml` 也转发给 Node（落在 `location /` 的反代里）。
哪天为了"省一次反代"把静态目录直出，这两个地址会 404，
而 404 的 robots.txt 会让爬虫退回"默认全允许、没有 sitemap"，通常不会有人发现。

**怎么验证**（实测过）：

```bash
npm run build
NUXT_API_BASE_SERVER=http://后端地址 PORT=3111 node .output/server/index.mjs
curl -s http://127.0.0.1:3111/robots.txt
curl -s http://127.0.0.1:3111/sitemap.xml
```

实测结果：robots.txt 里有 `Allow: /`、`Disallow: /admin` 与
`Sitemap: https://www.yigalaxy.xin/sitemap.xml`；sitemap 返回
`200 + application/xml; charset=utf-8`，内容是首页 + 后端当时真实的
两篇文章（`/article/117`、`/article/116`，各带 `<lastmod>`）。
把 `NUXT_PUBLIC_SITE_URL` 覆盖成 `https://test.example.com/` 之后，
canonical、sitemap 的 `<loc>`、robots 的 `Sitemap:` 三处**一起**变成
`https://test.example.com`（结尾斜杠也被归一化掉了）。

### 接口被限流为什么有两种提示（`app/utils/apiError.ts`）

后端有两个 `code = 429` 的场景，**含义相反、用户该做的事也相反**，
所以前端必须把它们分开 —— 混成一句的后果不是"不好看"，而是把人往错的方向指：

| 后端行为 | 含义 | 前端提示 |
|---|---|---|
| **真正的 HTTP 429** + `{code:429, message:"请求过于频繁，请稍后再试"}` | Resilience4j 的应用层限流拦下了这次请求（**登录 5 次/分钟**、前台文章列表 **300 次/分钟**） | 「请求过于频繁，请稍后再试」+ 登录按钮进入冷却 |
| **HTTP 200** + `body.code = 429` | 幂等键命中"另一个相同请求正在处理中"（用户把提交按钮点了两下 / 网络重试） | 「请求正在处理中，请勿重复提交」 |

- **怎么区分**：只能靠 **HTTP 状态码**。前者 `$fetch` 直接抛异常（HTTP 非 2xx），
  后者是正常返回 —— 光看 `body.code` 两者一模一样。
- **为什么 429 绝不能说成「网络异常」**（这次修的问题）：限流是"服务端好好的，
  只是让你等一会儿"，说成网络故障会让人去查网线、重启路由器，方向全错；
  而且重试的每一次都会往限流窗口里再记一次，越试越久。
- **为什么"处理中"要用另一句话**：这种失败只持续几十毫秒，用户只要**不重复点**就好了。
  如果说成"请求过于频繁，请稍后再试"，点了两下的人会以为第一次没成功，
  于是再点一下 —— 正好又撞一次幂等键。
- **登录冷却 10 秒**（`LOGIN_COOLDOWN_SECONDS`）：被限流后按钮禁用 + 显示剩余秒数。
  · **为什么必须禁用**：用户的第一个反应是"再点一次"，而每点一次都会再撞一次限流
    （还占掉 1 分钟窗口里的一个位置），点了没反应会让人以为按钮坏了。
  · **为什么是 10 秒**：后端是 5 次/分钟（60 秒窗口），1 秒的冷却等于没冷却；
    60 秒对"手滑输错几次密码"的正常人惩罚过重。10 秒能挡住连点、又不至于卡死。
  · **为什么还要在 `useAuth.login()` 里再拦一道**：`disabled` 只挡得住点击 ——
    登录弹窗里密码框的回车（`@keyup.enter`）和表单 submit 都能绕过按钮，
    所以真正的拦截放在发请求之前那一层。
  · **连点两下的竞态**：两次点击可能都通过了守卫，一次撞 429 打开冷却、另一次却成功。
    这时以成功为准（成功路径会清掉冷却），否则用户已经登录成功了还要空等 10 秒。

同一套语义由三处共同使用：`useApi`（所有请求的出口）、`useAuth`（登录）、
`app.vue`（按钮状态），判断依据与文案都放在 `app/utils/apiError.ts` 里，避免各写一份。

### 为什么关掉 `$fetch` 对 429 的自动重试（`app/utils/apiError.ts`）

Nuxt 的 `$fetch` 是 ofetch，它**默认会替我们再打一次**：
内置的重试状态码集合是 `[408, 409, 425, **429**, 500, 502, 503, 504]`，
而且对 `GET / HEAD / OPTIONS / TRACE` 这类无副作用的请求默认重试 **1 次**
（`POST / PUT / PATCH / DELETE` 因为可能已经落库，默认不重试）。

于是"后端正在限流"时会发生这件事：**前端立刻又打了一次。**
后果有两层，而且方向完全相反：

- **限流的目的就是降低压力，自动翻倍正好把压力加倍。** 后端登录限流是
  **5 次/分钟的整站配额**（Resilience4j 是进程级计数器，不区分请求来自谁），
  重试会再吃掉其中一个名额 —— 可能因此把"下一个本来能登录成功的用户"挤掉。
- **用户白等一个往返**，最后看到的还是同一句「请求过于频繁，请稍后再试」：
  提示没变，等待变长了。

所以 `useApi` 显式传了一份 `retryStatusCodes`（`API_RETRY_STATUS_CODES`）：
**ofetch 的默认值去掉 429。**

**为什么不是 `retry: 0`（一律不自动重试）**：429 与 5xx 的性质不一样，
不该一起处理 ——

| 状态码 | 含义 | 重试划不划算 |
|---|---|---|
| **429** | 服务端明确要求"你**不要**再发了" | **不划算**：再发是放大概率的 |
| 500 / 502 / 503 / 504 / 408 / 425 | 服务端自己也出了问题（网络抖动、短暂重启、GC 停顿） | **划算**：再发大概率能拿到正确数据，而失败一次用户就得自己刷新页面 |

SSR 的文章详情页尤其能说明问题：它等不到数据就是一个 500 错误页，
而重试一次可能就把正文正常渲染出来了。所以保留"临时性故障重试一次"，
**只摘掉 429** —— 也只改了 ofetch 默认值里的那一个状态码。

**⚠️ 必须传数组，不能传 `Set`（一个会静默失效的坑）**：ofetch 内部是这么判的 ——

```js
Array.isArray(options.retryStatusCodes)
  ? options.retryStatusCodes.includes(code)   // 传数组：用我们的
  : retryStatusCodes.has(code)                // 传别的：回落到它自己的默认集合
```

传 `Set` 进去 `Array.isArray` 是 `false`，它会**悄悄用回内置默认集合**：
429 照样被重试，配置看起来写了却一点没生效，而且不报任何错。

**这几条都由用例守着**（`test/useApi.nuxt.spec.ts` 里新增的 5 条）：
用**假的 fetch 实现**驱动**真 ofetch**，断言 429 只被调用一次；
并且配了一条**对照组** —— 把我们的配置摘掉之后断言它**确实会**被重试两次，
用来证明这个测试台真的能观察到重试（否则"假 fetch 只可能被调用一次"会让
第一条用例永远为真）。503 仍然重试、POST 不重试也各有一条。

> 顺带说明：这条改动只管"前端不再主动放大流量"，它**不是**限流本身。
> 真正的限流在后端（Resilience4j）与 Nginx，前端能做的是"别添乱"。

### 错误提示为什么要带追踪号（`X-Trace-Id`）

后端给每个请求分配一个 traceId，同一次请求的**所有日志行**都带同一个值
（Micrometer Tracing + Brave），并由 `TraceResponseHeaderFilter` 写进响应头
**`X-Trace-Id`**（不是 `X-B3-TraceId` —— B3 那套是跨进程传播用的，
回给浏览器的是这个自定义头；CORS 里也 `addExposedHeader("X-Trace-Id")` 了，
否则浏览器读不到）。

前端把它拼在错误提示后面：`操作失败（追踪号：abc123，报给站长可快速定位）`。
用户报一句"它坏了"没有任何线索，报一个追踪号就能让站长在日志里
搜出这次请求的全貌（参数、异常栈、耗时、上下游调用）。

- **只在错误提示里带**，成功提示不带：一串十六进制出现在"登录成功"旁边只是噪音。
- **没有这个头时什么都不加**：不能拼出「操作失败（追踪号：，报给站长）」这种空括号 ——
  用户会以为界面坏了，还会照着一句没有内容的话去反馈。
  断网这类"根本没有响应"的情况自然就没有号，那时也确实没法查日志。
- **怎么拿到响应头**（这是实现上唯一有点绕的地方）：`$fetch` 正常返回时只给 body，
  拿不到头；而错误里有一大半是 HTTP 200 + `code≠200`（业务失败），
  只在 catch 里读的话，这些最需要追踪号的错误反而没有号。
  所以用 ofetch 的两个钩子 `onResponse` / `onResponseError`
  （它们的 context 里带着 `response`）把这次请求的头记在闭包里，
  三种分支共用；catch 里再补一道 `err.response?.headers` 作为兜底。
- **值的形状也要卡一道**：`readTraceId()` 只接受"看起来像 traceId 的短 token"
  （字母数字与 `._-`，4–64 位）。这个值会被直接印在界面上，
  卡一道形状之后，就算哪天头被中间层塞进奇怪内容，也只会表现为"没有追踪号"。
- **返回值里的 message 只在后端给了原文时才带号**：调用方会自己兜底
  （`admin.vue` 的封面上传写的是 `res.message || '封面上传失败'`），
  我们若凭空塞一句「操作失败」进去，用户看到的提示反而变笼统 —— 这条契约有单测守着。

### 背景视频与音乐为什么不放在 `public/` 里

`bg-star.mp4`（背景视频）与 `bg-music.mp3`（背景音乐）现在放在仓库根目录的
**`static-media/`**，**不参与构建**；线上由服务器上的 Nginx 从 `/var/www/media/`
直接读磁盘提供。前端代码里的地址统一是 `/media/xxx`。

**为什么不留在 `public/`**：Nuxt 会把 `public/` 下的每个文件原样拷进 `.output/public`，
于是这两条音视频（合计 13.7 MB）被算作"前端产物"：

| | 挪出前 | 挪出后 |
|---|---|---|
| `.output/public` | 17.24 MB | **3.51 MB** |
| `.output`（整体） | 29.28 MB | **15.56 MB** |

体积只是表象，真正的理由有三条：

- **发布节奏被绑架**：改一行 CSS 也要重新构建、重新上传十几 MB，而这两个文件和代码毫无关系
- **它们本来就不该经过 Node**：视频要支持 Range 请求（拖动进度条），
  让 Nginx 直接读磁盘比让 Node 进程读文件再转发省事得多，也省掉一次内存拷贝
- **部署形态不一样**：前端容器是"构建产物"，媒体是"用户上传的数据"——
  混在一起之后，每次换背景视频都要重新构建镜像

**dev 与 prod 的 URL 必须一致**：地址由 `app/utils/media.ts` 的 `mediaUrl()` 拼一次，
默认前缀 `/media`，可以用 `NUXT_PUBLIC_MEDIA_BASE` 覆盖（哪天挪到 OSS / CDN 只改配置）。
页面里**不写死** `/bg-star.mp4` 这样的绝对地址 —— 否则就会出现
"本地能播（因为 dev 有同路径的兜底）、线上 404"这种最难查的差异。

**本地开发怎么还能播**：Nitro 里有一个**仅开发环境生效**的路由
（`server/routes/media/[...file].get.ts`）从 `static-media/` 读文件并流式返回，
所以 `npm run dev` 下 `/media/bg-music.mp3` 一样能取到（`curl -r 0-9` 能拿到
`ID3` 开头的前 10 字节）。

- **为什么它要显式判断环境**：生产构建之后，这段代码会被编译成"直接 404"
  （`test` 之外没有任何路径能读到磁盘）。这样万一 Nginx 的 `location /media/`
  配错、请求被转发进 Node，也只是媒体 404 —— 而不是让应用容器悄悄变成一个
  可以从磁盘读文件的服务器
- **安全**：文件名 → 磁盘路径的解析在 `server/utils/mediaFile.ts`（纯函数，有单测）：
  只接受纯文件名、拒绝 `..` 与隐藏文件、扩展名白名单（`.mp4` / `.mp3`），
  最后再校验"解析结果必须仍在该目录里"；不合法一律回 404（不告诉对方"你的穿越被识别了"）
- **Range**：支持 `bytes=start-end` / `bytes=100-` / `bytes=-500`，
  正常回 206 + `Content-Range`，起点越界回 416

**部署**：`static-media/` 里的文件要**手动（或由脚本）上传到服务器**，
`git push` 和 `npm run build` 都不会送它们过去：

```bash
sudo mkdir -p /var/www/media
sudo cp static-media/bg-star.mp4 static-media/bg-music.mp3 /var/www/media/
```

Nginx 里加上（**必须放在反代到 Nuxt 的 `location /` 之前**）：

```nginx
location /media/ {
    alias /var/www/media/;
    add_header Cache-Control "public, max-age=604800";
    access_log off;
}
```

细节（含"为什么 `public/` 里剩下的图不要挪过来"）见 `static-media/README.md`。

## 依赖的后端接口

前端共调用后端 **36 个接口中的 31 个**（没用的 5 个：
`GET /article/rss` 与 `GET/POST/PUT/DELETE /admin/category` —— 后者的界面还没接，
见下文「已知待办」里的「分类」一节）：

| 页面 | 调用的接口 |
|------|-----------|
| 首页 | `GET /article/page`（支持 `keyword` / `categoryId` / `tagId` 三条件叠加）、`GET /article/stats`、`GET /category/list`、`GET /tag/list` |
| 归档 | `GET /article/archive`（公开、走缓存；月份与月内文章都已排好序，前端不再排） |
| 文章详情 | `GET /article/{id}`（响应里带 `tags`）、`GET /comment/list`、`POST /comment`（**有限流**） |
| 登录 / 注册 | `POST /auth/login`、`POST /auth/register`、`GET /auth/me`、`POST /auth/logout` |
| 后台 · 概览 | `GET /article/stats`、`GET /user/page` |
| 后台 · 用户管理 | `GET /user/page`、`PUT /user/{id}/status`、`PUT /user/{id}/role`、`PUT /user/{id}/password`、`DELETE /user/{id}` |
| 后台 · 文章管理 | `GET /admin/article/page`、`GET /admin/article/{id}`、`POST /admin/article`（带 `tagIds`）、`PUT /admin/article/{id}`（带 `tagIds`）、`PUT /admin/article/{id}/status`、`DELETE /admin/article/{id}`、`GET /admin/tag/list`（标签下拉框的选项） |
| 后台 · 标签管理 | `GET /admin/tag/list`、`POST /admin/tag`、`PUT /admin/tag/{id}`、`DELETE /admin/tag/{id}` |
| 后台 · 评论审核 | `GET /admin/comment/page`（状态筛选，含 `email` 与 `ip`）、`PUT /admin/comment/{id}/status`（`?status=1\|2`）、`DELETE /admin/comment/{id}` |
| 后台 · 封面上传 | `POST /upload` |

> **分类的四个接口（`GET/POST/PUT/DELETE /admin/category`）前端一个都没用**：
> 后台的「分类」页目前是只读的，用的是公开的 `GET /category/list`
> （两个接口返回同样的形状，后端也说明了分类列表的写操作会让缓存立刻失效，
> 所以后台这份和前台那份一定是同一份最新数据 —— 再拉一次只是多一个要同步的 ref）。

> **标签是"覆盖式"语义**：`tagIds` 不传或传空数组，后端都会**清空**这篇文章的标签
> （先删关联、再按传入的 id 重建）。前端永远显式传一个数组（一个都不选就是 `[]`）——
> 两者结果等价，但显式传空数组把"我就是要清空"写进了请求体，
> 看日志的人才能分辨"用户清空了标签"和"前端忘了传这个字段"（后者是 bug，两者在后端看起来一样）。
> 传了不存在的标签 id 时后端返回 `404` 且**不会动到原有标签**（校验先于写入），
> 所以前端不需要先自己校验一遍 id 是否存在。
>
> **`GET /tag/list` 与 `GET /admin/tag/list` 形状一样，只差缓存**：
> 前台那份走缓存（标签是给访客看的导航），后台那份**不走缓存** ——
> 管理员刚建完标签就要能在文章弹窗里选到它，否则会怀疑"是不是没保存成功"。

## 已知待办

以下是当前版本明确存在、已列入计划的问题（详见后端仓库的 `TECH_ROADMAP.md`）：

**认证与状态**

- 刷新页面后靠 `GET /auth/me` 恢复登录态（`useState('user')` 刷新会丢），
  这一步是必要的；但 `/auth/me` 会额外查一次库，后端把它去掉更好
- **限流是"进程级"的，前端只能把提示与冷却做好**：后端用 Resilience4j 的
  RateLimiter，它是一个**进程内的计数器，不知道请求来自谁** ——
  所以登录的 5 次/分钟是全站共享的配额，别人多试几次可能把正常用户挤掉
  （实测就是"突然提示请求过于频繁"）。前端目前能做的只有：把 429 的提示说清楚、
  登录按钮冷却 10 秒、首页搜索加 300ms 防抖少打 `/article/page`（它有 300 次/分钟）。
  真正按 IP / 用户限流要放到网关或 Redis 层，属于后端路线图

> ✅ 曾经的「记住密码把**明文密码**存进 Cookie」已经修好了（安全审查里的 🔴）：
> 现在只记住**用户名**（勾选框也改叫「记住用户名」），密码不落任何客户端存储 ——
> 后端本来就没有"用记住的密码自动登录"的接口，存它除了留一个泄露点之外没有任何用途
> （cookie 不是 httpOnly，XSS 读得到；还会随每个同域请求发给服务端与代理，日志里是明文）。
> **老 cookie 也会被主动清掉**：挂载时只要发现 `rememberMe` 里带着用户名之外的字段，
> 立刻改写成只含用户名的值 —— 只改代码不改数据，等于没修。
> 这条由 `test/useRememberedLogin.nuxt.spec.ts` 的 16 个用例守着。

> ✅ 曾经的「退出登录没有调用后端」已经修好了：现在 `useAuth().logout()` 会先调
> `POST /auth/logout`（后端按 `jti` 把当前 token 拉黑，**旧 token 立刻失效**），
> 再清空 `token` 与 `user`，最后才跳回首页。接口失败也照样清本地 ——
> 用户点退出就是想离开，不能因为网络抖动把他困住。
> 同时删掉了原来指向不存在页面 `/login` 的死代码。

**数据不真实**
- 导航栏的「技术 / 读书 / 随笔 / 收藏 / 项目 / 友链」六个入口点了只弹"该页面开发中"
- 首页公告跑马灯是写死的宣传语（站点文案，不是接口数据），接后端公告模块之前不会变
- 首页文章的默认封面（`/cover-1~3.png`）是文章没填封面时的占位图，
  同一条文章换一页可能换一张 —— 它不是"真封面"，只是不让卡片破图

> ✅ 曾经的**首页三处硬编码假数据**（访客A / 访客B 两条假留言、「1 人正在看」、
> 三个编出来的曲名）已经全部删掉了：连面板、按钮、样式和只被它们用到的变量一起清掉，
> 没有留 `v-if="false"` 那种"藏起来"的写法。音乐卡片改成只播放真实存在的
> `static-media/bg-music.mp3`（一个音源、一个播放键、不写曲名；
> 它不参与构建，线上由 Nginx 的 `/media/` 提供）。
> 另外 GitHub / 邮箱 / RSS 三个"没有地址的入口"也一并删掉了。
> 这条现在由 `test/homeContent.nuxt.spec.ts` 的 11 个用例守着 ——
> 假内容被删掉之后没有任何工具拦着它被重新加回来。

> ✅ 曾经的「首页统计数字不真实」已经修好了：个人卡片的文章数 / 浏览量 / 分类数
> 改成走后端 `GET /article/stats`（口径：只统计已发布文章，由后端一条聚合 SQL 算好）。
> 改之前三个数字各有各的口径——文章数是"当前筛选条件下的篇数"、浏览量是
> "已加载的 12 篇之和"（点一次「加载更多」数字就变）、分类数是当前页列表长度。
> 接口失败时显示「—」而不是 0（0 会被访客当成"站点真的没有文章"）。

> ✅ 曾经的「`public/robots.txt` 只有两行、也没有 `sitemap.xml`」也已经修好了：
> 现在 `/robots.txt` 与 `/sitemap.xml` 都是 **Nitro 的运行时路由**
> （`server/routes/*.get.ts`），robots 里写明 `Allow: /`、`Disallow: /admin`
> 与**绝对地址**的 `Sitemap:`，sitemap 里是首页 + **全部分页取回来的已发布文章**。
> 之所以不做成构建时生成的静态文件：前端镜像的 `docker build` 里连不上后端
> （生成出来只会是空的），而且新文章发布后要重新构建镜像 sitemap 才会更新 ——
> 细节与实测见上文「sitemap.xml 与 robots.txt」。
> 这条由 `test/sitemap.nuxt.spec.ts` 的 38 个用例守着（含翻页边界与失败降级）。

> ✅ 曾经的「后台概览数字不可靠」也已经修好了：概览的四个数字改成**独立请求**
> （文章 / 浏览量 / 分类走 `GET /article/stats`，用户数走一次 `page=1&size=1` 的
> `/user/page` 只取总数），进入 `/admin` 就并行拉好、切到「概览」菜单再刷新一次。
> 改之前它们取的是用户表格 / 文章表格 / 分类列表的变量，而那些表格是"点开哪个菜单
> 才加载"的——直接进后台点概览，四个数字全是 0；在用户管理里筛了"禁用"，
> 概览的用户数又变成禁用用户数。
> 同时删掉了写死的「标签 0」那张卡片（后端还没有标签模块，摆一个永远不变的数字
> 只会让人误以为有这个功能），并把「后台骨架已就绪，等你对接后端接口」这句
> 开发期占位文案换成了对数据口径的说明。

> ✅ 曾经的「首页是纯客户端渲染、全站几乎没有 SEO 元信息」也已经修好了：
> 首页的数据从 `onMounted` 改成了 `useAsyncData`（服务端等数据回来再渲染，
> 文章列表直接出现在 HTML 里），全站补齐了 title / description / og / canonical /
> `lang="zh-CN"`，后台页带 `noindex`。
> 改之前的具体后果是：**详情页能被搜到、首页不能**（详情页一直是 SSR 的），
> 分享出去的链接没有卡片；而且只有详情页有 title，别的页面在浏览器标签上
> 都是同一个名字。这条由 `test/seo.nuxt.spec.ts` 的 20 个用例守着，
> 并由 `npm run build` 之后 `curl` 首页 HTML 实测过（片段见上文「怎么验收」）。

**SEO 与收录**

- **首页文章列表只渲染第一页**：服务端 HTML 里是第 1 页的 12 篇，
  后面的页要用户点「加载更多」。搜索引擎能收录的是"最新的一批"，
  更早的文章靠 `/article/{id}` 详情页被收录，没有做"列表分页 URL 可爬"这件事
  - 补一句：**`/archive` 现在把全部已发布文章的内链放在同一个页面里**
    （每一条都是真实的 `<a href="/article/{id}">`），所以"更早的文章要靠别处带进来"
    这件事已经好了一大截 —— 它和 sitemap 一起，构成"不依赖前端加载更多"的发现路径
- **没有 RSS / Atom 输出**（后端也没有这个接口），订阅只能靠收藏页面
- **文章详情页的 `description` 用文章摘要**：作者没写摘要时回落到站点描述，
  所以"没写摘要的文章"在搜索结果里长得都一样 —— 这属于内容侧的习惯问题
- **sitemap 每次请求都要翻页取一遍全部文章**：文章很多时（上千篇）会是一串
  连续的同源请求。目前靠 `Cache-Control: max-age=300` 扛住；
  真到那个规模应该改成"按需缓存到磁盘 / Redis"，或者让后端提供一个
  只回 URL 列表的轻量接口
- **sitemap 里没有 `<lastmod>` 的时区**（只写日期）：后端给的是
  `LocalDateTime`，没有时区信息；写日期形态是规范允许的、也最不容易出错，
  但精度就到"天"

**评论**

- **后台的评论审核界面这一批已经做好了**（列表 / 状态筛选 / 通过 / 拒绝 / 删除 / 待审核角标，
  见上文「后台的评论审核」）。在那之前，评论只能靠后端接口或 SQL 改 `status` 才可见
- **限流是 20 次/分钟的整站配额**（Resilience4j 是进程级计数器，不区分请求来自谁）：
  前端能做的是"提示说清楚、提交时禁用按钮别重复打"，
  真正按 IP / 用户限流要放到 Nginx 或 Redis 层。**没有**给评论按钮加冷却倒计时 ——
  这个配额是全站共享的，一个"你这台浏览器要等 N 秒"的倒计时既救不了你
  （别人照样把它耗光），也解释不了为什么等完还是失败
  - 实测还发现：**限流在业务校验之前就扣额度**（用必然 404 的请求连打 20 次照样把配额耗光）。
    这意味着"参数写错的请求"也会挤占正常用户的评论额度
- **⚠️ 转义范围比契约里写的宽**（联调实测）：契约说只转义 `<` `>` `&`，
  但实际上 `→` 会变成 `&rarr;`（`—` `…` `©` 这类 HTML4 具名实体同理），
  于是这些符号在页面上会显示成 `&rarr;` 而不是 `→`。
  **前端不能反转义**（那会把 XSS 防护一起拆掉），该在后端修：只转义五个 ASCII 特殊字符，
  或者改成"存原文 + 渲染时转义"。实测的输入与输出已逐字写在上文那一节里
- **`POST /comment` 的响应里 `createTime` 是 null**（联调实测）：后端插入后没有把
  数据库生成的时间回填到返回对象上，而列表接口返回的评论是有时间的。
  前端的做法是显示「刚刚」（刚提交的评论本来就是刚刚），
  没有去猜时间 —— 更彻底的修法在后端：插入后回查一次或让 `@TableField(fill=…)` 生效
- **评论没有"楼中楼"与编辑/删除自己的能力**：这是后端接口决定的（只有审核与删除，
  且都是管理员动作），前台只做展示与发表

**工程**

- **端到端（Playwright）测试还没有**：目前是组合式函数单测 + 九个页面组件测试
  （首页、首页内容真实性、**归档页**、文章详情页的标签、文章详情页的评论区、后台概览、后台文章弹窗的标签、后台标签管理、后台评论审核；组件测试覆盖了"页面把逻辑接上去了没有"，
  但跨页面跳转、真实后端联调还测不到）
- **`static-media/` 里的两个大文件仍然存在 git 仓库里**（约 13.7 MB，留在仓库里
  是为了"clone 下来就能复现同一份素材"）。如果以后素材变多、仓库变大，
  可以考虑 Git LFS 或干脆只留一份下载说明
- **媒体文件要手动上传到服务器**：`git push` 与 `npm run build` 都不会把它们送到
  `/var/www/media/`，目前只有文档（`static-media/README.md` + 上线检查表第 5 条）
  在提醒这件事，还没有脚本或 CI 步骤去核对
- **仓库还没有 GitHub 远程地址**，CI 工作流已就位但尚未真正跑过一次
- `app/pages/admin.vue` 单文件 **1845 行**，用户表格 / 文章表格 / 标签管理 / 评论审核 / 编辑器弹窗
  全挤在一个文件里（标签与评论这两批各加了三百多行 ——
  **"按模块拆成子组件"这件事已经不是"要不要做"而是"该做了"**：
  现在改一处评论审核要滚过一千多行别的模块才能找到它。
  顺带说明：这一行以前写的是 1001 行，那个数字早就过期了，这次按实际重新数过）

**分类（后端已有、前端没接）**

- 后台的「分类」页是**只读**的，但后端的分类增删改**已经做完了**
  （`AdminCategoryController`：`POST/PUT/DELETE /admin/category`，
  删一个"下面还有文章"的分类会被拒绝并告诉你还有几篇 —— 设计上比标签那种"物理删除 + 解除关联"更保守）。
  这一批任务的约定是"分类的写操作不在范围内"，所以前台没有接；
  要接的话，形状和标签管理完全一样（列表 + 弹窗 + 二次确认），接口已经能用了。
  另：`GET /admin/category/list` 与公开的 `GET /category/list` 返回同样的数据
  （后端说明了分类列表的写操作会推进缓存版本号，所以两边一定是同一份最新数据），
  所以后台这一页复用了已有的 `categories`，没有第二个 ref
- Element Plus 是全量引入（`app/plugins/element-plus.ts`），没有按需加载
- 用了 Element Plus 的 `el-dialog` 但**没有注入 z-index / id provider**，
  服务端渲染时控制台会刷一大堆 `ZIndexInjection` / `IdInjection` 警告
  （不影响功能，但把真正的报错淹掉了，该修）——
  和下面那条已修的 `el-link` 警告是同一类问题，修法也一样：找出新 API 换掉，
  再用断言把旧写法钉死

> ✅ 曾经的「`el-link :underline` 布尔写法触发 `ElementPlusError`」已经修好了：
> Element Plus 2.14 起 `underline` 的**布尔值**形态废弃，`<el-link :underline="false">`
> 每次渲染都会往控制台打一段 `The underline option (boolean) is about to be
> deprecated...`。它不影响功能，所以测试照样全绿 —— 危害恰恰在这里：
> stderr 每次都刷一遍，真正的报错混在其中就被忽略了。
> 现在两处链接（登录弹窗的「去注册」、注册弹窗的「已有账号？去登录」）
> 都写成新 API `underline="never"`，**行为完全等价** ——
> 组件内部本来就是 `isBoolean(underline) ? (underline ? 'hover' : 'never') : underline`，
> 也就是说这次只是把"它替我们做的转换"写成了显式值，
> 顺带让控制台彻底安静下来（实测：改之前每次跑测试都有 3 条，
> 改之后 `ElementPlusError` 在整轮测试输出里出现了 **0** 次）。
> 这条由 `test/linkUnderline.nuxt.spec.ts` 的 4 个用例守着，
> 其中包括一条"传布尔值确实会打警告"的对照组。

## 许可证

尚未指定许可证。若计划开源，建议补充一份 MIT 的 `LICENSE` 文件。
